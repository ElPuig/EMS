<!-- Working draft, not permanent documentation - one file per branch in changelog/, named
     after the branch, so multiple contributors' branches never conflict on this file when
     merged together. Once all of a branch's changes are folded into a target release branch,
     read every file under changelog/ to reconstruct the combined picture across contributors.
     The whole changelog/ folder gets deleted as the last step before merging into main.
     See CLAUDE.md's "PR changelog summary" section for how/when this gets appended to.

     This file is complete, ready-to-paste markdown - copy the whole thing as the PR body,
     no per-section extraction needed (that per-block-fence convention is for chat delivery
     only, where the developer already has the section headings in place on GitHub and pastes
     bullets under each one individually).

     COVERAGE NOTE (2026-07-30): this file currently covers everything from commit 4e1b342
     ("IBAN fix") through HEAD - the portion reconstructable with full fidelity from this
     session's own conversation/memory. It does NOT yet cover the ~80 earlier commits on this
     branch (main..4e1b342^, the "Fase 7"-"Fase 12"/"Fase A"-"Fase E" DTON rollout history) -
     the developer confirmed reconstructing those now wouldn't save any work over doing it
     once, in full, right before the PR is actually finalized (git history doesn't get harder
     to read later). When that time comes: walk `git log --oneline main..4e1b342^` and each
     commit's diff/message, prepend the resulting entries to this file (they predate everything
     below), then proceed as normal. -->

# Fixes

## `ems.planning` broke a genuine clean install (CI red on every PR):
- A fresh `-i ems` install (exactly what CI's `install.sh` does, and what this dev box's own `./upgrade.sh` never exercises) failed outright: `ems.planning`'s `check_ponderation` constraint fires the instant a planning row is created, but CSV has no equivalent of XML's inline one2many `eval` — a `data/custom/` planning's `planning_outcome_ids` can only arrive via a *separate* CSV file loaded afterwards, so the parent's own `create()` always saw zero outcome lines first and failed with "The outcome ponderation values must sum 100." Reproduced on an isolated scratch database (`sudo -u odoo createdb`, never touching the real dev `ems` DB) to confirm before fixing, matching the exact failure from the reported CI log. Fixed by skipping the check while `install_mode` is in context (the same flag Odoo's own data loader always sets, per `odoo/models.py::_load_records` — already the established pattern in this codebase for the IBAN-trust migration fix). Re-verified on a fresh install afterward: all 123 `data/custom` planning records load and their real ponderations sum to 100 (within float rounding noise, e.g. `99.99999999999999`) — the fix only reorders *when* the check runs, it doesn't mask any actual bad data.

## Portal IBAN renewal (bank account never trusted):
- The portal IBAN renewal route could mark a document "approved" without ever setting `allow_out_payment=True` on the underlying bank account, unlike the staff-side approval flow — 332 already-posted direct-debit invoices ended up with no usable bank reference as a result, out of 408 affected students. Portal renewal now trusts the bank the same way staff approval does; a migration backfills the 408 already-affected students, and invoicing now raises instead of silently self-granting trust.

## Authorization template matching semantics (level + study both required):
- A template scoped to both a level and a study matched if *either* condition held (OR), instead of requiring both (AND) — inconsistent with how templates scoped to only one condition already behaved. Unified to AND via a shared `_matches_scope()` predicate used both on create and on write.

## Enrollment header unique-per-course race condition:
- Two concurrent transactions could each pass the pre-check and create two live enrollments for the same student/course. Closed defensively with a partial unique index (`ems.planning`-style) plus a friendly error translation, even though 0 real occurrences were found in dev or production data.

## Enrollment header tutor guard (cross-study + write access):
- A tutor could be blocked from confirming their own tutored student's enrollment in some cases, or allowed to act outside their real scope in others. Fixed with a cross-study `@api.constrains` and by switching `_is_blocked_tutor` to Odoo's `has_access('write')` instead of hand-duplicating the access rule's own condition in Python.

## Enrollment junction duplicate constraint:
- `ems.enrollment` (student × group × subject) had no uniqueness constraint at the DB level. Added `UNIQUE(student_id, group_id, subject_id)`; a migration deduplicates the 21 pre-existing duplicate triples found in production (42 rows, confirmed field-identical within each pair).

## grade_session_remove missing still-enrolled guard:
- `_ems_sync_grade_session_remove` had no guard against removing grade lines for a still-enrolled student, unlike its sibling `_ems_sync_attendance_template_remove`. Added a shared `_ems_still_enrolled()` helper used by both, for symmetry, even though the gap is currently unreachable via the ORM thanks to the new junction constraint above.

## Student import wizard data-quality gaps surfaced, not silently dropped:
- Two known data-quality edge cases (no matching Esfera group, tutor row with no document number) were previously only noted in a code comment, invisible to the person running the import. Both are now surfaced via a new `stats['warnings']` block shown in the import result.

## LimeSurvey block special-filter mutual exclusion:
- `special_wpi_enrolled`/`special_subject_enrolled` were two independent Booleans that could both be checked at once, an invalid combination the UI didn't prevent. Replaced with a single `special_type` Selection (radio widget), with a migration backfilling existing data. Added the block's first browser tour, which caught two real, pre-existing (not new) bugs along the way: the tour's `select` action doesn't work on Odoo's `SelectionField` widget (`selectByLabel` needed instead — Odoo JSON-stringifies the option value), and `widget="code"` fields are an Ace editor, not a plain textarea (needs `ace.edit(...).setValue()`).

## Google Workspace welcome emails had a hardcoded, centre-specific domain:
- Both `mail.template` welcome emails (student and staff) hardcoded `@elpuig.xeill.net` in the subject and a link, even though `res.company.google_ws_domain` already exists as a proper per-company configurable field and the surrounding Python already used it correctly everywhere else. Any other centre installing EMS would have gotten Puig Castellar's own domain in these emails regardless of their own configuration. Fixed to read the same configured field (`env.company.google_ws_domain`) consistently in the templates and in four Python call sites that previously each had their own inconsistent `or 'elpuig.xeill.net'` fallback (one of the four even read a different field, `company_id` instead of `env.company`) — extracted into one shared `_gw_domain()` helper on `GoogleWorkspaceMixin` that raises a clear error if the domain isn't configured, rather than silently falling back to a literal that only made sense for one specific centre. Updated the Catalan/Spanish translations for the changed template text (subject + body) and added translations for the new configuration-error message.

## `ems.grade_session`'s grade-entry matrix now has its first browser tour:
- `widget="grade_matrix"` (the spreadsheet-like grid teachers use to enter outcome scores) had zero browser coverage since it was introduced — a clean `./upgrade.sh` and passing `TransactionCase` tests only proved the arch loaded and the ORM logic worked, not that the bespoke double-click-to-edit interaction actually renders and commits in a real browser. Added `ems_grade_matrix_entry` covering open session → double-click a cell → enter a score → Apply changes → re-render, asserting the persisted value directly against the DB afterward. Unlike a similar recent pass on `ems.limesurvey_block`, this one didn't surface a pre-existing application bug — it confirms the widget's core flow already works correctly.

## `ems.portal.access.wizard` now has its first browser tour:
- `widget="radio"` on the wizard's `mode` field (Grant/Revoke/Resend) had zero browser coverage. Added `ems_portal_access_wizard_revoke`, driven through the real UI path (select a student in the list, open the list's Actions/cog menu, launch the wizard, switch the radio to "Revoke access", Apply), asserting the underlying portal user is actually archived afterward. Surfaced a naming collision worth flagging, not fixing here: Odoo's tour-engine `:contains()` matching is case-insensitive, so this action's menu label ("Portal access (students/families)") and the native, unrelated Odoo action already on the same list ("Grant Portal Access") are ambiguous to a substring match — a real user reading the "Actions" menu has the same two similarly-worded entries to tell apart.

## `ems.grade_session_state_wizard` now has its first browser tour:
- The bulk evaluation-state-transition wizard's `widget="radio"` `mode` field (and its sibling `ems.grade_session_wizard`'s own, only ever smoke-tested) had never actually been clicked/switched in a real browser. Added `ems_grade_session_state_wizard_apply`, switching from the default "By study" to "By level", picking a level via the many2many_tags autocomplete, and applying a real state transition, verified against the DB afterward. Found one real, reusable tour-authoring gotcha: for a `target="new"` dialog's own form, its `<footer>` ends up a DOM sibling of `.o_form_view` (not nested under it) — a `.o_form_view footer ...` selector, which works fine for a directly-navigated page, silently never matches inside a dialog; scope through `.modal` instead for any dialog-rendered form's footer buttons.

## The enrollment ("matrícula") screen now has its first browser tour:
- `action_ems_enrollments` (the day-to-day `sale.order`-based screen secretary/admin use to manage enrollments) had zero browser coverage despite extensive backend `TransactionCase` coverage — none of that exercises the heavily xpath-customized native Sales form (many injected/renamed fields and notebook pages) in an actual browser. Added `ems_enrollment_form_tabs`: opens a seeded draft enrollment, walks every EMS-added tab (Enrollment Items / Authorizations / Payment) to confirm none crash on render, and does one real edit-save (the `shift` field) to prove the form is genuinely interactive.

## `ems.notice` now has its first browser tour:
- The bulk-email screen (`action_communication_list`) had zero browser coverage across three widget types that had never been driven in a real browser: `widget="html"` (rich-text editor), `widget="statusbar"`, and `widget="many2many_tags"` (whose onchange auto-populates the recipient list). Added `ems_notice_create_and_send`: creates a real notice end-to-end — picks a group, confirms the recipient list auto-populates, types the message body, saves, and clicks "Send now" through to a real `state` transition — verified against the DB afterward. Never at risk of a real send: `with_delay()` only queues a `queue.job` row in a normal test run, it doesn't execute it. Found one reusable gotcha: `widget="html"`'s contenteditable div isn't an `<input>`/`<textarea>`, so the tour engine's generic `edit` action can't fill it (`isEditable()` only recognizes those two tags) — Odoo's own tour engine has a dedicated `run: "editor <text>"` action built for exactly this (see `mail`'s own composer tours for the same pattern), used here instead.

## The daily attendance office screens now have their first browser tours:
- `ems.attendance_correction`, `ems.attendance_justification` and `ems.attendance_issue_tutor`/`_student`/`_status` (Attendances > Correction Requests / Justifications / Daily issues) had zero browser coverage despite daily use. Added three tours: `ems_attendance_correction_accept` (opens a pending request, accepts it, verifies the underlying `hr.attendance` was actually corrected), `ems_attendance_justification_open_and_edit` (walks all three read-only tabs of a seeded justification, edits+saves its Notes), and `ems_attendance_issue_drill_down` (a pure render smoke test across both nested report levels — tutor and student/session detail — since every view here is read-only by design). Found two reusable gotchas: the arch's `<header>` tag compiles to `.o_form_statusbar`, not a literal `<header>` element, so a `.o_form_view header ...` selector silently never matches (use `.o_form_statusbar` instead); and the same `HttpCase`-internal `self.session` naming collision from earlier in this batch recurred with a fixture named `cls.session`.

## The file-upload wizards now have their first browser tours:
- `ems.applicant_import_wizard`, `ems.student_import_wizard`, `ems.student_update_wizard` (`widget="binary"`) and `ems.working_schedules_import_wizard` (`widget="many2many_binary"`) had zero browser coverage — the generic tour "edit" action can't drive a file `<input>` at all. Added four tours using Odoo's own `inputFiles()` test helper: `applicant_import_wizard_tour.js` (uploads a real GEDAC-shaped CSV, imports a new applicant end-to-end, verified against the DB); `working_schedules_import_wizard_tour.js` (uploads a planner XML naming an unknown teacher e-mail via the cog-menu entry, verifying the wizard's own onchange correctly surfaces it as a blocking error); `student_import_wizard_tour.js` (uploads a minimal but structurally real xlsx — embedded as base64, since a valid xlsx is a zip container that can't be hand-authored as a plain string — missing most of the real Esfera columns, deterministically hitting the "missing required columns" validation as a real error dialog); `student_update_wizard_tour.js` (uploads a CSV, maps its columns via the dynamically-created `ems.csv_column` autocompletes, and updates a real student's name end-to-end). Found several reusable gotchas along the way: OWL doesn't sync an `<input>`'s live value to its HTML `value` attribute, so a plain `[value=...]` selector never matches — hoot-dom's `:value()` pseudo-class reads the real DOM property instead; a custom cog-menu item built directly from `<DropdownItem>` (bypassing Odoo's own cog-menu wrapper) renders as `.dropdown-item`, not `.o_menu_item`; and a tour that leaves a dialog open in edition mode at the end must explicitly close it, or Odoo's own test harness flags it as a failure.

## `res.config.settings`' EMS Management tab now has its first browser tour:
- The ~20-field "EMS Management" settings tab (Google Workspace, LimeSurvey credentials, course/attendance/strike settings) had zero browser coverage — admin-only and rarely touched, but a crash here would have high blast radius. Added `ems_settings_edit`: switches to the EMS tab and edits one representative field per settings block (proving the whole tab renders and its fields are genuinely interactive), then Discards rather than Saves. Deliberately does not exercise Odoo's own Save/reload mechanism: `res.config.settings.execute()` returns a `{'type': 'ir.actions.client', 'tag': 'reload'}` client action, which forces a real browser navigation rather than an in-place SPA save — a tour step cannot reliably observe anything afterward, since the navigation destroys the very JS execution context the tour macro runs in (confirmed empirically: a following step's trigger either raced the navigation, matching the still-live pre-reload DOM instantly with zero information about save status, or timed out on `.o_form_button_save:not(:visible)`, a signal that turns out to never apply to this view — Settings keeps Save/Discard permanently visible, unlike a regular form's dirty-only indicator). Verifying that generic Odoo mechanism is out of scope for an EMS-specific coverage gap; the fields rendering and accepting input is what this gap was actually about, and that's fully covered.

## Four more reference/config screens now have their first browser tours:
- `hr.job` (Job Positions), `hr.work.location` (Work Locations), `hr.contract.type` (Employment Types) and `ems.strike.reason` (Coexistence Reasons) had zero browser coverage — lower priority than the earlier batches (admin config data, plain widgets), but part of the same audit. Each got a plain create-CRUD smoke tour. One real finding along the way: `hr.job`'s EMS-added `employee_type`/`group_id` fields (models/employees/job.py) turned out to be unreachable through the form entirely — both sit inside native hr.job's "Recruitment" notebook page, which ships hardcoded `invisible="1"` in Odoo's own view (not a domain EMS could override), and the dev DB confirms they're populated exclusively via `data/cat/hr.job.csv`, never through the UI. Not a bug — consistent with this repo's established CSV-is-source-of-truth pattern for reference config — but the tour was scoped down to only what a user can actually do on this form (create by name) rather than testing a fictional workflow.

## Three more configuration screens now have their first browser tours:
- `resource.calendar`'s "Schedule Frameworks" screen, `product.template`'s "Enrollment Item" screen, and `sale.order.template`'s "Enrollment Templates" screen (all heavily xpath-customized over native Odoo forms) had zero browser coverage. Each got a create/edit smoke tour. Found the same reusable gotcha twice: both `hr.job` and `product.template`'s native `name` field render via `widget="text"` (a `<textarea>`, allowing multi-line values), not a plain Char `<input>` — a `.o_field_widget[name='name'] input` selector silently never matches on either.

## `ems.authorization.template` and the attendance-notification job monitor now have their first browser tours:
- `ems.authorization.template` (Authorization Forms, including its own `widget="html"` legal text) had zero browser coverage — a create-CRUD smoke tour. The `queue.job` attendance-notification monitor (a pure read-only log — jobs are only ever created automatically via `with_delay()`, never by hand) also had zero coverage — a render smoke test opening a seeded job. Found one gotcha: Odoo's list view binds the row-open click handler to a data cell, not the `<tr>` itself — clicking the bare `.o_data_row` does nothing.

## Enrollment collections and task assignment screens now have their first browser tours:
- `account.move.line`'s "Enrollment collections" report (pure read-only, list-only view_mode) — a render smoke test. `mail.activity.type`'s "Task Assignment" screen (an editable list letting admins assign who gets EMS's auto-created activities) — edits a real seeded row's assignee inline, verified against the DB.

## The "Families" filtered contacts screen now has its first browser tour:
- Closes the last item of the low-priority tour-coverage backlog. A plain create-CRUD smoke test on the "Families" res.partner action. Found EMS's own res.partner form (unlike native Odoo's single `widget="text"` name field, already seen elsewhere) splits it into separate `firstname`/`lastname` `<input>`s — the `partner_firstname` convention already used by the student/applicant import wizards.

## The daily roll-call ("Current" attendance) screen now has coverage for its core action:
- The single most-used screen in EMS — the "Current" pass-list client action (`ems_attendance_session_view`) teachers open every class period — already had its strike-issuing flow covered (`strike_tour.js`), but the actual core daily action (marking a student's attendance status) and starting a new session from a planned schedule slot had never been driven in a real browser. Added `ems_attendance_take`, covering the real workflow end-to-end: opens a planned (not-yet-started) schedule, starts the session (auto-populating enrolled students), marks one student's attendance status, adds a note to another, all verified against the DB. Status buttons only expose their translated name via the DOM (no stable status-id attribute) — clicking by column position rather than by label text keeps the tour correct regardless of UI language.
- Confirmed with the developer (not a bug): starting a session as a user whose own `hr.employee` isn't `employee_type='teacher'` (e.g. a pure admin account) fails with a "mandatory field not set" error on `session_teacher_id`, because `create_scheduled_session()`'s default derives the teacher from whoever clicks the button, not from the schedule's actual assigned teacher. Expected — only real teacher accounts start sessions in practice — so the tour logs in as a seeded teacher user rather than admin, matching real usage.

## `ems.attendance_template`'s weekly schedule setup screen now has a real end-to-end tour:
- The screen behind the roll-call above (Community > Configuration > Teachers, where a teacher's recurring weekly class slots are actually defined) only ever had a shallow color-widget smoke test — no tour had ever driven a real save, including its own embedded "Sessions" one2many tab (`ems.attendance_schedule` lines) or the `widget="daterange"` `start_date`/`end_date` fields. Added `ems_attendance_template_crud`: fills every field of a new template (teacher, level, study, group, space, subject, date range), adds a schedule line (weekday, space, start/end time) in the embedded list, saves, and verifies the persisted template and schedule against the DB.
- Resolves a stale in-code `TODO: not working, check how its solven at justification form` comment on the `start_date` field (`views/attendance/attendance_template/form.xml`): `widget="daterange"` turned out to work correctly with a plain tour `edit` action on the first attempt — it's built on the standard `DateTimeField` component and renders genuine `<input data-field="...">` elements, no special handling needed. This also resolves the same open question left deliberately deferred earlier in this batch's `ems.attendance_justification` tour (item above), which had avoided exercising the equivalent widget for its own "create new" flow.

## `ems.attendance_justification`'s "create new" flow now has coverage too:
- Now that `widget="daterange"` was confirmed working (previous item), went back and closed the deliberately-deferred creation flow for `ems.attendance_justification` as well — its tour previously only opened/edited an already-seeded record. Added `ems_attendance_justification_create`, filling `teacher_id` (whose onchange populates the `student_id` domain), `student_id`, and the date range, then saving, verified against the DB. Found one more daterange variant worth documenting: unlike `ems.attendance_template` (two separate `start_date`/`end_date` fields, each its own `widget="daterange"`), this form declares the widget only on `start_date` with `options="{'end_date_field': 'end_date'}'"`, which renders *both* date inputs inside that single field's own widget container (`.o_field_widget[name='start_date'] input[data-field='end_date']`, not a separate `[name='end_date']` container) — confirmed by reading Odoo's own `DateTimeField` template (`web.DateTimeField`). Also found that the typed value is stored exactly as typed, in UTC, regardless of the logged-in user's own `res.partner.tz` (Europe/Madrid here) — the headless test browser's own (UTC) timezone is what the client-side date parsing actually uses, not the Odoo user preference.

## Full UI-coverage re-audit (round 2) — two more bespoke client actions and the enrollment-proposal cluster now covered:
- After the "audit complete" declaration above was itself found incomplete three times in one day, the developer asked for a rigorous, mechanical re-check of the entire module rather than another memory-based read-through. A two-sided enumeration (every UI-reachable screen vs. every tour's actual reach, cross-referenced by hand and spot-verified against the XML/JS) found ~13 whole screens with zero tour coverage plus 2 unrelated navigation bugs (see Internal changes below) and several never-opened tabs on already-covered forms. Full findings and the proposed fix order are in `plans/missing_tour_coverage_audit.md`.
- **`ems.grade_tutor_matrix`** ("Evaluation for tutors") — a second bespoke OWL client action (same risk class as the daily roll-call widget), zero coverage before this. Added `ems_grade_tutor_matrix_entry`: opens the widget as a tutor, edits a grade cell, applies, and pages to the second student. Extracted the dblclick-to-edit interaction helper (previously duplicated verbatim in a follow-up commit had it been written twice) into a new shared `static/tests/tours/grade_matrix_helpers.js`, migrating the existing `grade_matrix_tour.js` to use it too — DRY, one helper for both bespoke grid widgets.
- **Enrollment proposal cluster** — the "Enrollment proposal" screen (`ems.action_student_group_enrollment`, a role-based `ir.actions.server` redirect) and both of its list-header bulk actions, `ems.enrollment_proposal_wizard` and `ems.graduation_wizard`, had zero coverage. Added `ems_enrollment_proposal_create` (select a student, open the proposal wizard, pick a template, confirm the destination group auto-suggests via `_onchange_suggest_group`, create the enrollment) and `ems_enrollment_proposal_graduation` (mark a student graduated), both verified against the DB.
- **Preinscription** (`action_ems_applicants`) — zero coverage, including an applicant's own form (whose default-open tab, "Applicant data", had never rendered) and the same "Enrollment proposal" wizard exercised here via its auto-preselected-template code path (an applicant with a granted course) rather than the manual one already covered above. Added `ems_applicant_form_and_proposal`, verified against the DB.
- **Students without destination** (`action_students_no_destination`) — zero coverage of the list or its "Suggest destination group" bulk action. Added `ems_no_destination_suggest_group`, verified against the DB. Both this and the previous two items needed the same fix for a flaky first attempt: this dev DB has 1000+ real matching records, and searching the list then immediately clicking a same-named group header (matched against the still-unfiltered initial render, a race the tour engine's own trigger-polling doesn't protect against since the stale element already exists) intermittently grabbed the wrong group. Fixed by waiting for the search's own facet chip to render before acting further, and preferring to drop the default grouping facet entirely over expanding same-named nested group headers.
- **Academic history** (`action_year_record_list`) — zero coverage of the standalone list/form, the nested subject→outcome popup dialogs, and the separate "Academic history" tab rendering the same data on `res.partner`'s own form. Added `ems_year_record_list_and_subject` and `ems_year_record_partner_tab`, both render-only (the model is `create="0"`, records only ever come from `generate_for_students()`, called directly in the test setup the same way `tests/test_year_record.py` already does). Two more reusable gotchas found: clicking a bare `.o_data_row` inside a *field-level* `readonly="1"` embedded list does nothing — same "row-open is bound to a data cell, not the `<tr>`" rule already documented for cog-menus, apparently true here too; and switching a kanban view to list (`action_student_kanban`) needs an explicit wait for `.o_list_view` to actually render before typing into the search box, or the input event fires on the stale kanban-view search node.
- **Plannings** (`action_planning_tree`) — zero coverage. Added `ems_planning_crud`. Found (the hard way, via a screenshot-driven debugging session) that `ems.planning`'s `subject_id` onchange auto-populates `planning_outcome_ids` with one row per outcome, evenly split to sum 100 — a subject with a single outcome ends up with that row already at 100% the moment the subject is picked. The tour originally added a line by hand on top of this, silently ending up with two rows and a "must sum 100" save error; fixed by just asserting the auto-populated row instead.
- **Import grades** (`action_grade_import_wizard`) — zero coverage. Added `ems_grade_import_wizard_missing_sheet`, reusing the same base64 xlsx fixture already established by `student_import_wizard_tour.js` (any valid-but-wrong-shaped xlsx deterministically hits the wizard's own "no gradeable rows" validation, proving the upload + import button work without needing to replicate Esfera's full export format).
- **Work placement evaluation (EM)** (`action_em_grading_wizard`) — zero coverage, including its own bespoke `widget="em_matrix"` grid (simpler than `grade_matrix`/`grade_tutor_matrix`: plain `<input>` cells, no dblclick-to-edit floating input needed). Added `ems_em_grading_wizard_apply`, verified against the DB. Found a load-dependent flake only visible in the full, unscoped suite run (never in isolation): `action_apply()` re-fetches and re-renders the whole matrix from the DB, and ending the tour immediately after clicking "Apply changes" could race that reload under heavy load, tripping Odoo's own "tour finished with an open form view in edition mode" failure. Fixed by waiting for the applied score to be confirmed present after the reload before ending the tour.
- **Minutes** (`action_minute_tree`) — investigated, **not actually a gap**: its `<menuitem>` has been commented out since the module's very first commit, deliberately unexposed (a whole notebook section of its form is commented out too) — skipped rather than adding a tour for a screen no real user can reach.
- **Providers** (`action_provider_kanban`) and **Enrollments (config)** (`action_enrollment_tree`, `ems.enrollment` — distinct from the well-tested `action_ems_enrollments`/`sale.order` "Enrollments" screen, same name, different model) — both zero coverage. Added `ems_provider_crud` and `ems_enrollment_config_crud`.
- **Surveys → Recipients tab** (`ems.limesurvey_header`'s `limesurvey_recipient_ids` tab and `ems.limesurvey_recipient`'s own add-student popup) — zero coverage; `limesurvey_block_tour.js` only ever opened the sibling "Blocks" tab on the same header. Added `ems_limesurvey_recipient_add_student`, verified against the DB.
- **Native `hr.attendance` form's EMS additions** (the "Request Correction" statusbar button and "Corrections" stat button, `views/attendance/attendance_correction/hr_attendance_form.xml`) — no EMS tour had ever opened the native check-in/check-out form at all, so `attendance_correction_tour.js`'s existing "accept an existing request" flow never actually exercised how a request gets created in the first place. Added `ems_attendance_correction_request`, verified against the DB.
- **ASP secondary actions** (`action_asp_kanban`, `action_asp_role_tree`) — same view architecture as the well-tested Teachers screens, but their own domain/context had never been opened. Added `ems_asp_crud`/`ems_asp_role_crud`. Found a real, deliberate EMS validation rule the hard way (a `.o_field_invalid` DOM probe, since the save silently no-opped with no visible dialog): `private_email` is required for any *new* teacher/ASP employee (`views/community/employee/form.xml`, needed for the Google account recovery address) — it lives on the "Private Information" tab, not the default one, so the failure gave no visual clue on the visible screen.
- **6 notebook tabs on already-covered forms, never actually clicked by their tour**: `ems.level`'s "Studies" tab, `ems.study`'s "Subjects"/"Attached files" tabs, `ems.authorization.template`'s "Data Fields" tab, `ems.group`'s reinforcement-type "Students" page (a second, separate `<page>` from the main-type one already covered), `res.partner`'s "Former student" tab (reachable via `withdrawal_tour.js`'s existing archive flow, since `action_apply()` converts `contact_type` to `'withdrawal'` immediately — no new fixture needed), and `ems.attendance_template`'s "Students" tab. Extended the existing tours in place rather than adding new ones.
- **The entire portal frontend** (`controllers/portal_*.py`, reached via `@http.route`, a different reachability mechanism than the rest of EMS) had zero coverage beyond one narrow `url_open` check for the IBAN-renewal action. Added:
  - 5 genuine browser tours (`portal_tour.js`, one per page-rendering route: `/my/gestion-matriculas`, `/my/documentacion`, `/my/comunicaciones`, `/my/account`, `/my/asistencia`+`/my/calificaciones`), logged in as a real portal (student) user — confirming Odoo's tour-test JS does load on portal/website pages (`web.__assets_tests_call__`), same mechanism as the backend. Found the same real gotcha twice: this dev DB's real portal users default to Catalan, so an English-text `:contains()` trigger silently never matches — switched to structural selectors (icon/class based) instead, the same language-independence discipline already used for backend status buttons elsewhere in this module.
  - 7 `url_open`-based checks (`test_portal_actions.py`) for the remaining state-changing action routes (`select_student`, `portal_enrollment_authorize`, `portal_enrollment_confirm`, `portal_authorization_document`, `portal_documentation_submit`/`cancel`/`download`) — a pure POST/GET endpoint doesn't need a full browser tour to prove it doesn't 500, matching the bar the one pre-existing portal test already set. Found that `sale.order`'s own authorization-template auto-sync (on write/create) can collide with a manually-added `(0, 0, {...})` authorization command for a scopeless template — fixed by reading the auto-created line instead of adding a duplicate.

## The last isolated popups/buttons now have tour coverage (Tier 5, plan complete):
- `open_exception_popup` (`ems.attendance_issue_status` and its twin on `ems.notice.line`), `open_error_popup` (`ems.limesurvey_recipient`), and `action_limesurvey_delete_closed_confirmed` (the RedirectWarning confirmation flow for deleting a closed survey) had zero browser coverage. All four now have real tours, closing `plans/missing_tour_coverage_audit.md`'s Tier 5 in full - every tier of that audit (1 through 5, plus Portal) now has a tour. The exception/error popups reuse each model's existing test fixtures (a session line marked missed, a sent notice, a manually-created recipient), force-setting the already-linked `queue.job`'s `state`/`exc_info` (or the plain `error` field) directly in Python rather than simulating a real async failure. The delete-confirmation tour drives the full real path: select row → Actions → Delete → generic confirm dialog → server-side `RedirectWarning` → Odoo's own `RedirectWarningDialog` → its primary button (labelled with EMS's own "Yes, delete permanently") → verified the record is actually gone afterward. Found one reusable gotcha: `widget="text" readonly="1"` renders as plain text, not a `<textarea>`, so the `textarea:value(...)` trigger used by the file-upload wizard tours doesn't apply here - match visible text with `:contains(...)` instead.

## Cog-menu import wizards and the `res.partner` strike stat button now click-tested via their real UI path:
- `applicant_import_wizard_tour.js`/`student_import_wizard_tour.js`/`student_update_wizard_tour.js` previously opened their wizard by direct URL to its own action — proving the wizard form itself works, but not that the real click path (the list's cog/Actions menu entry) actually opens it. All three now navigate to the list, open the cog menu, and click the real entry (`.dropdown-item:contains('...')` — a raw `<DropdownItem>` renders Bootstrap's own class, not Odoo's `.o_menu_item`) before the rest of the existing flow. Also added a fourth leg to `strike_tour.js` covering the `res.partner`-side "Strikes" stat button (`action_view_strikes`, `views/community/contact/form.xml`) — a different method than the already-tested `attendance_session_header`-side twin, previously unclicked. Found a reusable tour-authoring gotcha along the way: after switching a kanban view to list, the search box must not be touched until an explicit wait for `.o_list_view` confirms the transition finished, or the typed text races the search bar's own remount and is silently lost (1111+ real students in this dev DB means the un-searched target is never on page 1, so the search silently no-opping reads as "record not found" rather than an obvious search failure).

## `ems.student_import_wizard` (Import from Esfera) now has a real success-path browser tour:
- Its only existing tour deliberately used a minimal fixture that always hit the "missing required columns" error - a real successful import through the actual upload UI (not just the underlying `action_import()` Python method, already covered by an existing `TransactionCase` test) had never been driven in a browser. Added `ems_student_import_wizard_success`, uploading a full-`_REQUIRED_COLUMNS` xlsx via the real cog-menu path and verifying the created student/family/deduced relation afterward. Also added a `TransactionCase` test proving the wizard tolerates the *full* real-world Esfera export shape (86 columns, not just the ~35 it reads) without breaking - built from a real, anonymized sample export the developer shared (see Internal changes below for the anonymization process), never committed or retained.
- One of the two suspected gaps from the real-data inspection turned out not to be a gap at all, corrected after the developer's colleague pushed back: `TIS` documents (Targeta d'Identificació Sanitària) already map correctly to `res.partner.medical_id` (`docs.get('TIS')`, right after the `document_id`/`passport_id` lines in `_process_row`) — the original report missed that line on a too-quick read. Locked it in with a new test (`test_process_row_tis_document_type_maps_to_medical_id`, also covering the multi-document case: DNI + TIS on the same row). The other gap is still open, pending a concrete example from the developer (see `plans/student_import_wizard_esfera_gaps.md`): a `Contacte ... - Valor` column that can carry 3-4 `" - "`-joined parts in real data while `_parse_contact_value` only reads the first 2, silently dropping the rest — could be extra tutors or extra phone/email channels for the same one, needs the real shape to know which fix applies.

# Internal changes

## Real Esfera export anonymized to unblock testing, then the original deleted:
- The developer shared a real Esfera/SAGA export (119 real minors) to close the `student_import_wizard` tour gap. Never read directly - a script opened it, replaced every identifying column (name, RALC, document number, phone, email, full address) with synthetic values while keeping categorical/structural columns (group code, document type, nationality) for realistic testing, and printed only aggregate non-personal stats (column names, date-format distribution, distinct categorical values) - never a real value. The real file was deleted immediately after (not gitignored - the developer wanted it gone outright, not just excluded from version control), and the anonymized scratch copy was deleted once its structure was captured into the new tests above; no xlsx binary of any kind is stored in the repo, following this module's existing convention (`grade_import_wizard`'s tests) of generating xlsx fixtures programmatically at test time instead.

## Two navigation bugs found during the UI-coverage re-audit, fixed regardless of tour work:
- **`menu_work_locations` id collision:** `views/community/work_location/menu.xml` and `views/community/working_schedules/menu.xml` both declared `<menuitem id="menu_work_locations">` — manifest load order silently let "Working schedules" overwrite "Work locations", so the latter has had no menu entry at all (while the action/views themselves worked fine, reachable only by direct URL). Renamed the work-locations one to `menu_hr_work_locations`; both now coexist.
- **`ems.action_student_form` xmlid collision:** declared twice with the identical id — once properly (`contact/menu.xml`) and once with a hardcoded, unreferenced `res_id="57"` (`group/form.xml`, the load-order winner). Deleted the dead duplicate. Confirmed empirically that Odoo's data loader does *not* reset a field simply because a later definition omits it (the stale `res_id=57` survived in the DB until the surviving definition explicitly declared `<field name="res_id" eval="False"/>`) — a real, reusable gotcha for future xmlid-collision fixes.

## `data/custom/` fully migrated to the `__import__.` xmlid prefix:
- Closed a long-standing backlog: all 140 remaining XML records under `data/custom/` (`ems.planning`, `ems.course`, `ems.authorization.template`, `ir.sequence`) converted to CSV and rescoped to `__import__.`, so an EMS module upgrade can never again silently delete centre-specific configuration. One file (`ems_enrollment_template_opt.xml`) stays XML as a confirmed, permanent exception — its `product_id` is resolved via a dynamic domain search with no static external id to reference, which CSV cannot express.
- Along the way, fixed a real migration bug found only by testing against a genuine `./upgrade.sh` run (not the test suite, which happened to bypass it): the IBAN-trust backfill migration would have failed on a real production upgrade because `res.partner.bank`'s trust check explicitly refuses `SUPERUSER_ID` unless `install_mode` is set in context.
- Also fixed a second, unrelated latent bug surfaced by the same testing: one `ems.course` row had a legacy `NULL` boolean (from before the field existed on the model) that a plain data-file resync could never have corrected on its own, since Odoo's ORM reads `NULL` and `False` as equivalent — backfilled explicitly via a one-time migration.
- Documented the exact, empirically-verified mechanics of Odoo's data-loading system (the `__import__` module-ownership check vs. the separately-stored `noupdate` flag are two independent mechanisms, easy to conflate) in a new technical reference (`docs/en/developers/shared/data_loading.md`), including a capability table of what XML can do that CSV genuinely cannot (and vice versa) and a decision framework for `noupdate=True` vs `False`, cited against Odoo's own official documentation.

## `data/main/`+`data/cat/` XML files reviewed against the same criteria:
- Applied the same "XML only where CSV genuinely can't" discipline to the remaining XML files outside `data/custom/`. Six of seven converted to CSV; two of those were deleted outright rather than converted 1:1 (`ems.job_group_relationship.xml`/`ems.role_group_relationship.xml` only ever existed to add one field to records already declared elsewhere — folded directly into the existing files instead).
- Reviewed all three files that were `noupdate="1"` for undocumented reasons. Two (`ems.mail_activity_type.xml`, `res.partner.category.xml`) had no real justification and are now `noupdate=False`/CSV — EMS can freely improve cosmetic/business-label content it fully controls. One (`ems.schedule_framework_default.xml`) turned out to be this centre's own live, in-use bell-schedule template (traced via its actual consumers, not assumed) and correctly stays `noupdate=True`/XML.
- Moved two `mail.template` records that were sitting directly in `data/` (outside all three documented subfolders) into `data/main/` as CSV, alongside the domain-hardcoding fix above.

## PR checks made self-explanatory (manual commands visible without clicking in):
- `.github/pull_request_template.md` gained a "Before merging" checklist naming `/deploy-check` and `/clean-changelog` with what each does - previously only discoverable by reading the workflow source.
- The PR checks list itself didn't make clear either command needed a manual comment: `require-changelog-clean.yml`'s workflow-level `name:` now embeds the command directly (`"Require changelog/ removed before merge (comment /clean-changelog to fix)"` - GitHub renders `"<workflow name> / <job name> (<event>)"`, so this shows the command inline without touching the job id/name that branch protection's required-check rule actually matches on). `deploy-check.yml` needed a different fix since it posts a plain commit status (not a check-run) - added a job that posts an immediate `pending` status with a `description` explaining the manual comment, replacing GitHub's generic "Expected — waiting for status to be reported" with actual instructions.

## CI test suite parallelized (~12 min → dynamic shards, was a single sequential job):
- `ci-unit-testing.yml` ran all 166 test classes (1418 tests) in one sequential job/process - a full local run measured at exactly 12 minutes, of which the ~2 minutes were regular `TransactionCase` tests and the rest was the 68 browser-tour (`HttpCase`) classes alone, each spinning a real headless Chrome + HTTP server. Split into a matrix: one "fast" shard (everything except tours) and N "tour-*" shards (tour classes round-robin split), all running in parallel - `.github/scripts/compute_test_shards.py` discovers tour classes from `tests/*_tour.py` at CI run time (this repo's own existing naming convention), so a newly added tour file is automatically picked up without editing the workflow. The "fast" shard also skips installing Chrome entirely, since it runs zero browser tests.
- `test.sh` extended (backward-compatible - `./test.sh TestClassName` unchanged) to accept a full `--test-tags` expression when the argument starts with `/` or `-` (e.g. `'/ems:A,/ems:B'` or `'-/ems:A,-/ems:B'`), which is how each shard selects its subset. Verified locally before touching any CI YAML: both the comma-separated inclusion and pure-exclusion `--test-tags` syntaxes behave exactly as documented (confirmed via a real, scoped local run - `0 failed, 0 error(s)` selecting only 2 named classes out of the full suite).
- Kept a dedicated `ci` job (unchanged name) that aggregates the matrix shards' result and is the only thing branch protection's existing required check needs to see - a matrix job's own check name changes per shard (`"Test (tour-2)"`, etc.), which would otherwise silently orphan the existing required-check configuration in GitHub (main's branch protection references the literal name `ci`, per `require-changelog-clean.yml`'s own comment) and block every future PR with a check stuck "Expected, never reported." No changes needed on the GitHub UI side because of this.
- This repo's GitHub repo is public, so the extra Actions minutes from each shard's duplicated install step are free - only wall-clock time was being optimized here, not cost.
