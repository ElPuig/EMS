# What's new:

## Working-schedule import wizard simplified (no more per-teacher file upload, no more silent auto-archiving):
- Removed the per-employee scoped file upload (`teacher_id`/`file` fields, the Schedule tab's "Import" button) — a teacher joining mid-year now gets their schedule via the tab's own existing "New" panel (blank framework or copy from another teacher) or by hand, never a single-file XML upload. The batch importer (`attachment_ids`, the "Working Schedules" list's cog menu) is the only way in.
- A conflicting active session found during import (same space/day/time, different subject or group) is no longer silently archived: it now **blocks** the import with a clear error naming the conflicting session, in both the onchange preview and `create()`. Legitimate co-teaching (same subject, sharing a group) is left alone and surfaced as a non-blocking confirmation banner instead.
- This was possible because groups are permanent and reused across academic years (confirmed via `353-add-course-transition-wizard-setup-next-course`, merged in this branch) — the course transition wizard archives a study's outgoing attendance templates before its groups are ever reimported, so a fresh import genuinely never writes on top of already-populated data for its own scope; any active overlap it finds is always either co-teaching or a real problem, never something to guess-and-archive.
- `ems.attendance_template.find_external_conflicts` renamed to `classify_external_conflicts`, now returning `(co_teaching, space_conflicts)` instead of a single recordset to silently archive.
- Found while verifying the redesign against the real, incremental (department-by-department) import workflow: reusing the Schedule tab's own `sync_from_schedule_batch`/`_reconcile_teacher_groups` for the batch importer silently archived a shared teacher's already-imported *other* department schedule the moment a second department's file mentioned that same teacher again — zero error, pure data loss. Fixed with a dedicated `_reconcile_fresh_import`/`sync_from_schedule_batch_fresh_import` write path (no pre-scan of a submitting teacher's untouched other combos) and `ems.teaching.sync_from_schedule(..., replace=False)` for the importer's own call — the Schedule tab's live-editor methods are untouched.
- New `ems.attendance_template.find_self_conflicts`: a teacher imported in this same batch, double-booked against their own already-active schedule for a genuinely different subject/group (e.g. two departments' files scheduling them at the same time), now surfaces as its own named blocking banner/error in the wizard, instead of falling through to Odoo's raw, unworded `check_overlap` constraint error.
- See `docs/en/developers/employees/working_schedule.md` for the full design; `plans/working_schedule_import_redesign.md` tracks what's left (a group/teacher correction dropdown for unresolved codes is designed but not yet implemented).

## Working-schedule import wizard becomes a guided, multi-step flow (skeleton + first screen):
- The single-screen wizard is being rebuilt into a 7-step guided flow (a statusbar showing Welcome → Resolve groups → Resolve teachers → Internal conflicts → Existing schedule conflicts → Pending teachers → Existing teachers), so each class of problem gets its own screen instead of a wall of banners all at once. **Only the first screen ("Welcome", file upload) has real behavior in this pass** - the middle 5 steps are placeholders that just advance the statusbar for now (their own resolution screens are separate, not-yet-built work - see `plans/working_schedule_import_redesign.md`); the final step's "Import" button already does the real work, unchanged in spirit from before.
- Nothing is written to any model until the final "Import" click: leaving the Welcome screen parses every attached file and caches the result - the actual `resource.calendar`/`ems.teaching`/`ems.attendance_template` writes only happen once the flow reaches the last step.
- `create()` is back to a plain, un-overridden default - previously it did the entire import as a side effect of the wizard's very first save, which would have made the *first* "Continue" click on the new intro screen perform the whole import prematurely.
- Found and fixed a real Odoo gotcha along the way: a `type="object"` button whose method returns nothing gets silently converted into "close the window" by the web client - which closed this wizard's dialog the moment the very first "Continue" click saved the record. Fixed by having the button's handler always return an explicit action re-opening the same wizard record.
- **Welcome screen simplified to just the file upload, no validation at all (developer feedback right after trying it):** the first version of this screen still showed the old wizard's four banners (unknown e-mail, unresolved group/subject, missing classroom and room conflicts in red; pending-identification codes in blue; already-has-a-schedule/co-teaching in yellow), live-updated via an onchange as files were attached - but resolving those problems is exactly what steps 2-3 are meant to do, so surfacing (or blocking on) them here pre-empted those screens' own purpose. Now "Continue" activates the moment any file is attached, full stop; every real problem is deferred all the way to the final "Import" click instead (today, since steps 2-6 aren't built yet, that's where it surfaces - an unresolved e-mail during this pass's testing showed as a plain error dialog at Import time rather than as a banner on the welcome screen). The one exception: an unresolvable subject/group *code* still blocks leaving the welcome screen, since parsing that row produces no data at all to defer.
- **Continue/Import/Cancel moved from the arch's `<header>` to a `<footer>` (developer feedback: they were showing up alongside Odoo's own generic Save/Discard, not in their place):** the wizard's `<header>` only had the `state` statusbar field to begin with, but its 3 action buttons were also declared there - since a `target="new"` dialog with no `<footer>` in its arch falls back to Odoo's own generic Save/Discard/Remove buttons in the modal's actual footer area (`web.FormView`'s `layout-buttons` slot, portaled straight into `.modal-footer`), that generic row kept showing up alongside our own statusbar buttons instead of being replaced by them. Moving the 3 buttons into a real `<footer>` (keeping only the `state` field in `<header>`) makes Odoo use ours instead of the fallback, matching how every other wizard in this codebase already does it (e.g. `ems.grade_import_wizard`). An earlier attempt at this same complaint incorrectly targeted `web.FormStatusIndicator` (a *different*, unrelated "unsaved changes" control-panel icon that isn't even what was rendering here) - reverted once testing showed it changed nothing.
- **Screen 2 ("Resolve groups") is now real, the second of the 7 steps to get actual logic:** an unrecognized `<Students>` name in an uploaded file no longer blocks the Welcome screen - it's deferred to this new step instead, one line per distinct unresolved name (the same typo appearing many times across the batch is one correction, not one per occurrence), each with a dropdown to pick the real group (creating one on the spot is allowed, same as any other group field). A batch with nothing to resolve just shows a confirmation message instead of a list. This required a genuine design decision, checked with the developer first rather than assumed: the Welcome-screen redesign above had left a comment saying an unresolved group "has no later step (yet) that could resolve it" - several already-passing tests took that as a permanent rule, when it was actually only true until this exact screen got built. Confirmed with the developer, then adapted those tests to the new, deferred behavior instead of leaving the screen unbuilt. Subject *codes* are unaffected by this change - still no resolution screen for those, still block at Welcome, same as before.
- User docs (`docs/{en,ca,es}/admin/working-schedules.md`) rewritten to match the current wizard end to end (Welcome → Resolve groups → placeholder steps → Import) and to drop the old per-teacher import instructions, which no longer exist (removed by the batch-only change earlier in this same changelog) - these sections had gone stale across this branch's earlier phases and were never caught until now.
- **"Continue" now renders enabled/disabled instead of appearing/disappearing (developer feedback: "que no te deje continuar hasta que no se hayan seleccionado grupos... creo que quedará más claro si los botones de continuar aparecen como enabled o disabled"):** a "Resolve groups" screen with an unpicked group now shows "Continue" grayed out (not clickable) rather than blocking only after the click, and rather than hiding the button outright the way the Welcome screen used to. Since Odoo's own buttons have no native per-record "disabled" expression, this is built from two buttons sharing the same visual spot - the real one and a cosmetic, always-disabled twin - only one of which is ever actually present, toggled by a new computed `continue_disabled`. Verified in a real browser tour, not just reasoned about: asserts the disabled twin is actually there before a group is picked, and the real one right after.
- **Screen 3 ("Resolve teachers") is now real, the third of the 7 steps to get actual logic:** an unrecognized teacher e-mail no longer waits until the final Import click to fail - it's deferred to this new step instead, one line per distinct unresolved e-mail, each with a dropdown to pick the real teacher (creating a brand-new one is deliberately **not** allowed here - that's still only ever automatic, for a not-yet-hired post code, at the final Import step). A batch with nothing to resolve just shows a confirmation message instead of a list, same "Continue" enabled/disabled behavior as the groups screen. Unlike screen 2, this one needed no developer check-in first - the plan already fully specified this exact behavior, so building it was an unambiguous, intended consequence: the existing "unknown teacher" tour and test were adapted the same way screen 2's were, since an unknown e-mail now surfaces here instead of as an error dialog at Import.
- **Screen 4 ("Internal conflicts") is now real, the fourth of the 7 steps to get actual logic - the first genuinely new check in this redesign, not just a relocated existing one:** two different teachers in the same batch ending up scheduled in the same classroom at the same time now surface here as a resolvable pair instead of failing unworded (or, for the exact-match co-teaching shape, being silently merged with no confirmation at all). Each pair is classified and defaults accordingly: **co-teaching** (same subject and group - kept as-is, the existing merge logic already handles it correctly, this screen only adds visibility/confirmation) defaults to keeping both; a **split ("desdoble") session** (same subject, different groups sharing a room) defaults to **reassigning rooms** (two classroom pickers, both pre-filled with the colliding room, so at least one must actually be changed to resolve it); a genuine **room conflict** (different subjects) only offers picking one side to keep. Checked the UI/validation shape with the developer first, per the plan's own "Complexity flag" for this exact step: a flat dropdown with all options always visible, validated server-side on Continue, rather than a fancier widget hiding invalid options per row.
- User docs (`docs/{en,ca,es}/admin/working-schedules.md`) updated again for the new "Resolve teachers" step, alongside "Resolve groups".
- **Screen 5 ("Existing schedule conflicts") is now real, the fifth of the 7 steps to get actual logic:** the same classification/resolution shape as screen 4, but against an already-active `ems.attendance_schedule` from a *different* import (or a genuine double-booking of the same teacher across two incremental imports) instead of another entry in this same batch - previously always raised unconditionally on any such collision (no matter how legitimate), now shown and classified the same way: co-teaching (kept as-is), a split ("desdoble") session against an existing room (defaults to reassigning rooms), or a genuine conflict (defaults to keeping the new import's own data, archiving the conflicting existing session). No developer check-in needed before building it - the plan already fully specified this screen, including the trickiest part: reassigning an *existing* session's room has to go through the already-existing `ems.attendance_mixin._write_or_new_version()` helper (archive + clone with the new room) instead of a raw field write whenever that session already has real attendance history, a distinction resolved during an earlier same-day session's model-level work, well before this screen's own implementation began.
- Found and fixed while testing this screen: "left prevails" (the new import's data wins) only archived the *specific colliding schedule line*, not its parent template - if that was the template's only active line, it was left behind as an orphaned, empty record. Now the now-lineless template is archived too, matching the plan's own "archives/trims the existing session's template" wording.
- User docs (`docs/{en,ca,es}/admin/working-schedules.md`) updated again for the new "Existing schedule conflicts" step.
- **"Resolve teachers" (screen 3) gains a "Create new teacher" checkbox, superseding the "creating a brand-new one is deliberately not allowed here" note above (developer feedback after using the wizard for real):** some unresolved e-mails turn out to belong to a genuinely never-hired teacher, not a typo/mismatch of an existing one - forcing a pick from the (create-disabled) dropdown made no sense for those. Ticking **Create new teacher** locks the dropdown and creates a brand-new pending-identification teacher for that e-mail at Import, exactly like a placeholder code (`X1`, `X2`...) - except the file's own e-mail is kept and pre-filled as an editable **Work email** (**Assign corporate email manually** ticked), rather than left for a later "Generate Google account" to auto-generate, since the developer's own framing was "the e-mail can't be trusted as confirmed, but it's worth trying". Extracted `_get_or_create_pending_teacher(identifier, manual_email=False)` out of `_apply_import`'s previously-inline placeholder-code branch so both paths (a placeholder code, and now a create-new-ticked e-mail) share the exact same get-or-create-by-`schedule_import_code` mechanism, dedup-safe on re-import either way.
- User docs (`docs/{en,ca,es}/admin/working-schedules.md`) updated again for the new "Create new teacher" checkbox.
- **Follow-up polish round (2026-08-06, developer feedback after using every screen for real):**
  - The "Create new teacher" checkbox's label was too wide for its column - shortened to **"New"**. Centering only the checkbox (not the column header, which Odoo's list renderer has no arch-level hook to align) looked mismatched, so both stayed at Odoo's default left alignment instead - no custom CSS needed.
  - The "Internal conflicts"/"Existing schedule conflicts" screens' `kind` column was labeled "Type" - renamed to **"Conflict"**, reading more naturally next to the two colliding sides' own labels.
  - A genuine same-room `plain_conflict` (two different subjects colliding in the exact same classroom) now defaults to **"Reassign rooms"** instead of "Left prevails", pre-filled with the group's own currently-assigned classroom on both sides (same treatment split-session conflicts already got) - picking a room is the actual fix, not an afterthought. A **self**-conflict (the same teacher double-booked in *time* against their own existing schedule, in a genuinely *different* room) keeps the old "Left prevails" default with no room pickers, since reassigning rooms fixes nothing when the real problem is the teacher needed in two places at once, not a shared room.
  - Each conflict row is now tinted by urgency: yellow for co-teaching/split-session (asking for a quick confirmation), red for a genuine room clash (needing an actual decision) - built from Odoo's own `decoration-warning`/`decoration-danger` row attributes (colored text only) plus a small scoped CSS file adding a soft background tint on top, since Bootstrap's `.text-bg-*` utility classes turned out to render with no visible effect at all in this Odoo install (verified via a real screenshot before committing to that approach).
  - The wizard's dialog widened to `extra-large` (Odoo's largest built-in dialog size, via `context: {dialog_size: "extra-large"}` on the cog-menu's `doAction()`) - the conflict screens' 6-column lists were crowding badly at the default width. One dialog serves every step, so this affects the whole flow, not just the conflict screens.
  - Added the wizard's first screenshots to the admin manual (`docs/{en,ca,es}/admin/working-schedules.md`): the Welcome screen, the "New" checkbox, and a color-tinted conflict row - captured via the existing browser-tour infrastructure, then cropped to exclude the underlying list view's real employee names from the background (never publish a screenshot with real personal data still visible, even dimmed/partial).
- **Second follow-up round (2026-08-06, same-day developer feedback after trying the first round for real):**
  - **"New" now defaults to ticked** on "Resolve teachers" - a genuinely never-hired teacher turned out to be the more common real case, so an admin who needs to pick an existing teacher now unticks it, rather than the other way around. Several backend tests and 3 tours needed adjusting for the new default (a row that used to need explicit `create_new = True` now needs explicit `create_new = False` to test the "neither set" case; one tour had to end with Cancel instead of a stray click, after discovering mid-edit clicks don't reliably blur/save an x2many list row - "Tour finished with an open form view in edition mode").
  - "Internal conflicts" state renamed to **"File conflicts"** (clearer against "Existing schedule conflicts") - the technical `state` value and model name were deliberately left unchanged (no migration needed, purely a display-string rename).
  - `left_label`/`right_label` headers now read **"File"/"File"** on the "File conflicts" screen (both sides are file entries) and **"File"/"Database"** on "Existing schedule conflicts" (the right side is the already-existing session) - a per-view `string=` override, no model change.
  - "It's co-teaching (keep both)" shortened to **"Confirm"**, and "Split session (different room needed)" shortened to **"Split session"** - both too long for the now-narrower columns.
  - **"Confirm" only appears as an option for a co-teaching-eligible row now**, not for e.g. a "Room conflict" row (picking it there was already rejected server-side, but it was still confusingly offered) - Odoo's Selection field has no declarative per-record way to filter its own options within the same list, so this needed a small, genuinely necessary custom widget (`ems_conflict_resolution`, `static/src/js/backend/conflict_resolution_selection_field.js`) extending the standard one to filter based on the row's own `kind`.
  - Column widths rebalanced: the two label columns capped narrower, `kind`/`resolution`/the two room pickers given more room - via CSS classes on the `<field>` (plain `w-*` Bootstrap classes turned out to have no effect on a list's data-cell width in this Odoo version).
  - **Found and fixed a pre-existing i18n gap while renaming these same strings:** every `kind`/`resolution` option's translation had only ever referenced `internal_conflict_line`'s own copy, never `external_conflict_line`'s - "Existing schedule conflicts" had been showing every conflict-kind/resolution label in English regardless of app language since screen 5 was first built. Fixed by adding the missing model reference to all 7 affected `.po` blocks, verified directly against `ir_model_fields_selection`.
- **Third follow-up round (2026-08-06, same day, developer feedback after the second round's column headers read ambiguously):** "File conflicts" screen's `left_label`/`right_label` disambiguated to **"File (left)"/"File (right)"** (both sides are file entries, so plain "File" twice was ambiguous), with matching **"Classroom (left)"/"Classroom (right)"** for the room pickers; "Existing schedule conflicts" kept its unambiguous **"File"/"Database"** as-is, with matching **"Classroom (file)"/"Classroom (DB)"**. "Split session (different room needed)" further shortened to **"Split session"**. Also removed the `min-width` CSS added for the previous round's column-width rebalancing - it turned out to actively misalign the room-picker columns once combined with a real classroom's long name, and served no purpose once the two label columns were already capped by their own `max-width`.
- **Screens 6 ("Pending teachers") and 7 ("Existing teachers") are now real (2026-08-10) - the last two of the 7 steps, both purely informational, no resolution needed:** by the time the flow reaches them, every teacher identifier in the batch is already fully resolved (screens 2-3 already forced that) - all that's left is showing the admin, before the real "Import" click, exactly what it's about to do with each teacher. "Pending teachers" lists every not-yet-hired post code and every e-mail left with "New" ticked - each one about to become a brand-new pending-identification employee. "Existing teachers" lists every teacher already matched to a real employee (whether auto-matched by e-mail or corrected on screen 3) - a heads-up that this import is about to update their schedule/subject assignments. Either screen shows a confirmation message instead of a list when nothing of that kind is in the batch.
  - New shared `_classify_teacher_item()` (`ems.working_schedules_import_wizard`) is the single source of truth both these previews and the real `_apply_import()` write path branch on - extracted from what used to be `_apply_import`'s own inline `if/elif` chain, so the previews can never silently drift out of sync with what Import actually does. Both new fields (`pending_teachers_html`/`existing_teachers_html`) are plain `Html`, not a dedicated line model like every earlier resolution screen - there's nothing to edit here, so a full O2M + security entry would be unused ceremony.
  - Extended 2 existing browser tours (pending-teacher, resolve-teacher-email) with real assertions on both screens' content instead of just clicking through a placeholder; fixed the stale "placeholder step" wording across every other tour that also passes through these two states.
  - User docs (`docs/{en,ca,es}/admin/working-schedules.md`) updated for the two new steps.

## "Pending teachers" moved earlier in the flow, and the final step expanded into an "Overall summary" (2026-08-10, developer feedback after using the two new screens above for real):
- **"Pending teachers" moved from after "Existing schedule conflicts" to right after "Resolve teachers"** (*"El paso 6, ¿no puede hacerse tras 'resolve teachers'...?"*): traced the actual data dependency first (`_classify_teacher_item` only reads data screen 3 already fixed - neither conflict screen writes anything it depends on), confirming the move was safe with no hidden coupling, and reads better narratively (new teachers shown before conflicts involving them are resolved). `_STATE_SEQUENCE` is now `intro → groups → teachers → pending_info → internal_conflicts → db_conflicts → summary`.
- **The final step ("Existing teachers") renamed to "Overall summary" and expanded into a real end-of-wizard recap**, after a short design back-and-forth: first just a clearer label (*"'existing teachers' creo que no es claro. 'Override info' me parece mejor"*), then - after confirming there's no actual per-teacher keep-vs-override choice anywhere in this wizard, contrary to the developer's assumption - settled on expanding this same final screen (rather than adding an 8th step) into a genuine pre-Import confirmation: with 6+ steps and no way to go back, one last purely-informational recap of everything the run resolved is worth having. New `overall_summary_html` (`Html`) lists a count each for unresolved group names, teacher e-mails, pending teachers, file conflicts and existing-schedule conflicts resolved, plus existing teachers affected - reusing the wizard's own already-populated line O2Ms/cached items, no new computation. The pre-existing existing-teacher preview stays on this same screen, now as a supporting detail under the counts rather than the screen's whole content.
- **The technical `state` key was renamed too (`override_info` → `summary`), not just the label** - a deliberate departure from this wizard's own established precedent of keeping the technical key stable through a label-only rename (e.g. "Internal conflicts" → "File conflicts" kept `internal_conflicts`): the developer explicitly asked for the key to track the label here (*"que la clave técnica coincida con la etiqueta, puede estar resumida... sino nos liaremos"*), to avoid the two names diverging confusingly mid-conversation.
- Extracted `_teacher_preview_items()`/`_bullet_html()` out of what used to be `_teacher_preview_html`'s own inline logic, so the new counts summary and the pre-existing teacher previews share the same dedup/rendering helpers instead of duplicating them.
- All 8 browser tours in `working_schedules_import_wizard_tour.js` updated to match the new screen order (the "Pending teachers" step's click/assertions relocated to right after the "teachers" step in every tour that passes through it) and the `summary` key/label rename; `TestWorkingSchedulesImportWizardTour` (8/8) and `TestWorkingSchedulesImportWizard` (78/78) both green.
- `docs/en/developers/employees/working_schedule.md` restructured to match: "Pending teachers" is now documented as Screen 4 (moved ahead of the two conflict screens, which shift to Screens 5-6), and the final screen is documented as Screen 7 ("Overall summary"), with the full feedback trail preserved for context.
- User docs (`docs/{en,ca,es}/admin/working-schedules.md`) updated for the "Overall summary" rename and its new counts-recap content.
- i18n: real Catalan/Spanish translations added for the `summary` state's own label and the 6 new count-message strings, verified directly against `ir_model_fields_selection` via `psql` (not just the `.po` diff).

## Every wizard screen now explains itself, and "Welcome" was tightened (2026-08-10, developer feedback: *"Quiero una breve introducción o resumen de lo que sucede en cada paso... quiero que el usuario lo tenga muy claro"*):
- Before this, only the "Welcome" screen had a plain intro paragraph - every later screen jumped straight into a success alert or a resolution list with no explanation of what that screen checks or why. Since this is a long, strictly-linear flow with no way to go back once past a step, all 6 remaining screens ("Resolve groups", "Resolve teachers", "Pending teachers", "File conflicts", "Existing schedule conflicts", "Overall summary") now open with the same plain, unstyled intro paragraph "Welcome" already used, stating what that screen checks and what to do about it.
- Also simplified "Welcome" itself, per the same feedback (*"la sección wellcome se pueda simplificar... si parte de su contenido se reparte entre los otros"*): its original paragraph did two things at once - explained the import, and warned that running it against a course already in progress can create conflicts needing resolution "in the following steps". Split accordingly: "Welcome" keeps the general framing (what this import does, when to run it, plus a new explicit reassurance that nothing is written until the final Import click) - the specific "why a DB conflict can happen" explanation moved into "Existing schedule conflicts"'s own new intro, where it's actually relevant instead of a forward-reference.
- Purely a text/view change - no new field, no new logic, `_STATE_SEQUENCE`/`state` untouched. `TestWorkingSchedulesImportWizardTour` (8/8) and `TestWorkingSchedulesImportWizard` (78/78) both re-confirmed green.
- User docs (`docs/{en,ca,es}/admin/working-schedules.md`) gained one sentence noting each screen explains itself on-screen, ahead of the existing numbered walkthrough (kept as the detailed reference, not rewritten).
- i18n: the 7 new/changed intro paragraphs (Welcome's rewritten text + 6 brand-new ones) got real Catalan/Spanish translations - verified directly against `ir_ui_view.arch_db`'s jsonb (`ca_ES`/`es_ES` keys present with the actual translated sentences), not just the `.po` diff.

## "Overall summary" reworked into per-category detail blocks, not just counts (2026-08-10, same day, developer feedback: *"Creo que la última pantalla, la del resumen, no se entiende demasiado... Si se ha resuelto 1 grupo, quiero saber cómo... por bloques horizontales, bien diferenciados"*):
- The "Overall summary" screen's 6 count sentences ("1 unresolved group name(s) resolved", etc.) now each head their own card, with that category's concrete detail lines directly underneath - a resolved group name now shows which real group it was matched to, a resolved teacher e-mail shows which real teacher (or that it's becoming a new pending teacher), and each conflict shows exactly how it was resolved (kind, resolution chosen, and the two rooms when "Reassign rooms" was picked). Laid out as a responsive, wrapping horizontal row of Bootstrap cards (`d-flex flex-wrap gap-3` - already-bundled utilities, no bespoke CSS) instead of one flat vertical list, so a count and its "how" are never disconnected and the whole recap is visually distinct block-by-block.
- The standalone `existing_teachers_html` field (previously the only place the "existing teachers affected" preview lived on this screen) was removed - folding that same preview into its own block would otherwise have shown it twice on the same screen. Its own dedicated success alert was removed too, replaced by the same "Nothing to show here." placeholder every other now-empty block already uses.
- New `_selection_label()`/`_conflict_detail_line()`/`_summary_block_html()`/`_summary_blocks_html()` helpers on the wizard model; `kind`/`resolution` labels are resolved via the Selection field's own `convert_to_export()` (the ORM's translation-aware idiom), not the field's raw untranslated `.selection` list.
- 5 new backend tests covering each block's own detail content (group/teacher resolution, a create-new row, a room-reassignment conflict, the empty-block placeholder) plus a dedicated Catalan-translation test - genuinely useful here since this Odoo version reads Python `_()` code-string translations straight from the module's own checked-in `.po` file at runtime (no database round-trip, unlike field/view translations), so there's no DB column to `psql`-verify against the way there is elsewhere; a functional check under a real `lang` context is the only way to actually prove a new code string translates. One pre-existing tour selector (`existing_teachers_html`'s own widget) updated to check the new block content instead.
- `TestWorkingSchedulesImportWizard` (82/82) and `TestWorkingSchedulesImportWizardTour` (8/8) both green.
- Dev doc (`docs/en/developers/employees/working_schedule.md`) updated to match. Per a separate, related process change agreed the same session (see CLAUDE.md's "Close" step), the three-language admin manual and any screenshots for this screen are deliberately **not** updated yet - held back until the developer confirms this block-based design is the final shape, rather than rewriting it again if more tweaks follow.
- **Layout tweak, same day, right after actually seeing the blocks rendered**: the first attempt packed all 6 cards into a wrapping horizontal row (several per line) - too cramped in the first row of 4 (*"La primera fila tiene 4 tarjetas y se ven muy apretadas... vamos a poner una tarjeta por fila"*). Changed to one full-width card per row (`d-flex flex-column` instead of `flex-wrap`) - still plain Bootstrap utilities, no bespoke CSS either way.
- **"Existing teacher(s) affected" block gains a one-time explanatory note** (*"deberíamos aclarar cómo se verán afectados"*): the count and list of names alone didn't say what "affected" actually means for them. New optional `note` parameter on `_summary_block_html()`, shown once above the names, explaining that their weekly schedule gets synced with the file's content and their attendance templates get updated in place (no session history yet) or archived-and-replaced by a new version (real history already exists) - a block-level generality, not a per-teacher prediction, since which outcome applies to which specific line is only actually decided by the real sync's own diff at Import time.
- **Conflict detail lines: the kind now stands out with an arrow instead of parentheses** (*"se veria mejor destacando el co-teaching... con una flecha en vez de paréntesis"*): `"%(left)s vs. %(right)s (%(kind)s): resolved as %(resolution)s"` → `"%(left)s vs. %(right)s --> %(kind)s: resolved as %(resolution)s"` - once seeing a real, long left/right pair rendered showed the parenthesised kind blending into the rest of the line.
- **"Pending teacher(s) will be created" block gains its own explanatory note too** (*"que quede claro que significa que son 'pending' y que después se les podrá cambiar el nombre, crear su cuenta, etc."*): explains once, above the list, that these are placeholder employees created now so their schedule/subjects are ready immediately, and that afterwards each one's own record gets its real name/personal e-mail filled in and its Google account generated via the pre-existing button - same mechanism as any other new teacher.

## Reason-aware "Archived" ribbon for students and teachers, plus a new student Expulsion outcome:
- Archived students now show **why** at a glance instead of a generic "Archived": a green **Alumni** ribbon (graduated), an orange **Withdrawal** ribbon, or a red **Expelled** ribbon — on both the form and the kanban card.
- Archived teachers show their actual departure reason the same way (**Retired**, **Resigned**, a new **Transfer** reason for teachers reassigned to a different centre, or **Fired**), also on both form and kanban — teachers' kanban previously showed nothing at all for an archived employee.
- A single reusable field widget (`ems_archived_reason_ribbon`) drives all of this for both models, reading a plain label + hex color pair (`archived_reason_label`/`archived_reason_color`) computed per model — see `docs/en/developers/contacts/contact.md` and `docs/en/developers/employees/employee.md`.
- `hr.departure.reason` gains a `color` field (same hex color-picker widget already used for `ems.attendance_status`/`ems.role`), so an admin can pick the ribbon color for each departure reason, including any new ones added later.
- New: the withdrawal wizard now asks the admin to choose between **Withdrawal** and **Expulsion** when archiving a student (voluntary vs. administrative withdrawal stays a free-text note, not a separate option). The confirm button's label adapts to the choice ("Withdraw"/"Expel"). Expulsion always results in `contact_type = 'expelled'`, even for a student who had already graduated once.
- A full audit of every place `contact_type`/`exit_type` was filtered or branched on (menu domains, search filters, `transition_status`, Google Workspace account suspension, `_academic_result()`, category sync) was done before adding the new `expelled`/`expulsion` values, to make sure none of them silently missed the new case.

## Pending-identification teachers from schedule imports:
- Importing a working-schedule file (per-teacher or the general cog-menu importer) no longer fails when a row names a not-yet-hired post with a placeholder code (e.g. `X1`, `X2`) instead of a real e-mail.
- A "Pending teacher (X1)" employee is created automatically, with the schedule/subjects/attendance lists already assigned — no manual setup needed.
- Re-importing an updated file for the same still-unstaffed post (same code) updates that same employee in place, never creating a duplicate.
- These employees show a "Pending identification" indicator (Teachers list column, kanban + form ribbon in a soft, non-garish yellow), with a matching search filter/group-by. The kanban indicator started as a badge under the name; changed to a ribbon (2026-08-01) to match the other reason-aware ribbons added to that same kanban right after it.
- Resolving a pending teacher's real identity reuses the existing "Generate Google account" button: fill in the real name + personal email, click the button — no separate confirmation step.

## "Pending teachers" screen merged into "Resolve teachers" (2026-08-10, developer feedback after a real import: *"¿Ves factible fusionar los pasos 'Resolve teachers' y 'Pending teachers' en 'Resolve teachers'? ... me encuentro que me han puesto al mismo profe de forma diferente, por lo que deberíamos tener cubierto que haya asignado a un docente más de una vez, sería la misma persona"*):
- A bare placeholder code (`X1`, `X2`...) used to fall straight through to Import with no correction row at all — only an already-known e-mail could ever be manually reassigned to an existing employee. This meant the same real teacher sent under two different codes (or a code and a mistyped e-mail) always minted two separate pending employees, with no way to say "these are the same person" before Import.
- Fixed by merging the old, separate "Pending teachers" preview screen into "Resolve teachers": `_pending_teacher_identifiers()` now lists BOTH e-mail-shaped identifiers and bare codes with no matching employee, each getting the same correction row (**New** ticked by default either way). Assigning the SAME existing employee to two different rows already resolves correctly with no further change needed — `_continue_from_teachers()`'s own `identifier → employee` map has no uniqueness constraint on the employee side — confirmed by a new dedicated regression test, not just by reading the code.
- The wizard is now a 6-screen flow instead of 7 (`_STATE_SEQUENCE`: `intro → groups → teachers → internal_conflicts → db_conflicts → summary`) — the `pending_info` state, its `pending_teachers_html` preview field, and the now-unused `_teacher_preview_html`/`_bullet_html()` helpers (plus their dedicated `ems_wizard_bullet_list` CSS) were all removed outright. `_classify_teacher_item()`'s fate classification survives unchanged, now feeding only the "Overall summary" screen.
- A dedicated "same person as" cross-reference field was analyzed and offered for the rarer "both sides are genuinely new" case, but explicitly declined for now (*"creo que de momento esto que pido es más sencillo y cubre el 99% de los casos"*) — not implemented.
- All 8 browser tours updated (the merged "pending_teacher" tour now asserts both placeholder codes render as their own correction row on "Resolve teachers", instead of a separate preview); `TestWorkingSchedulesImportWizard` (86/86) and `TestWorkingSchedulesImportWizardTour` (8/8) both green.
- Dev doc (`docs/en/developers/employees/working_schedule.md`) restructured to match: the former Screen 4 section is now a short "merged" note, and the two conflict screens + "Overall summary" renumbered down by one (5→4, 6→5, 7→6).
- User docs (`docs/{en,ca,es}/admin/working-schedules.md`) updated for the merged screen and the renumbered steps that follow it.
- i18n: the "Resolve teachers" screen's intro paragraph, success message, field label and validation message all changed text (now covering codes as well as e-mails) — real Catalan/Spanish translations added, verified directly against `ir_model_fields`/`ir_ui_view.arch_db`'s jsonb via `psql`. The two now-dead `pending_info` screen strings ("Nothing to resolve here..."/"No new teacher will be created...") were removed from both `.po` files rather than left orphaned.

# Changes:

## Attendance templates now support several studies at once, lock their identity fields once real attendance exists, and get a "New version" action (`plans/attendance_template_multi_study.md`):
- `ems.attendance_template.level_id` removed entirely - never used in any report, and always derivable from `group_ids`/`study_ids`. `study_id` (`Many2one`) is now `study_ids` (`Many2many`): a template built for co-teaching/"desdoble" can genuinely span groups from more than one study, and `_write_schedule_sync` now unions every involved group's own study instead of keeping only the first one's.
- New `_check_subject_valid_for_all_studies` constraint: `subject_id` must actually be taught in **every** one of the template's `study_ids` (an intersection, backed by the new computed `allowed_subject_ids`), not just one of them.
- Once a template has real attendance history (new computed `has_sessions`, both on `ems.attendance_template` and `ems.attendance_schedule`), its identity fields become readonly: `teacher_ids`/`subject_id`/`study_ids`/`group_ids` at the template level, and each schedule line's own `weekday`/`space_id`/`start_time`/`end_time` at the line level. Editing them in place after real roll-calls exist would retroactively misrepresent what those sessions were actually about.
- Correcting a locked template (or a single schedule line) is now done via a new **"Edit"** button instead (icon `fa-pencil-square-o`; internally `action_new_version()` - labeled "New version" until 2026-08-05, renamed since from the user's side it reads as editing a locked record, not something that needs to advertise the archive+clone happening underneath - the confirm dialog still spells that out): it archives the template/line (cascading to its schedule lines for the template-level action) and opens a fresh, fully editable clone with no session history - already-taken attendance stays linked to the archived original. Archiving happens before cloning (not after), since cloning first would momentarily collide with the still-active original under the existing same-teacher/same-room `check_overlap` constraint.
- Also added icons to the template form's header buttons ("Reload students": `fa-refresh`) and fixed the schedule line's row button visually overlapping with the list row's own delete ("x") button - switched to `class="oe_stat_button"` (icon-only + tooltip), the same pattern already used for row buttons elsewhere in this codebase (`content_ids`/`outcome_ids`/`criteria_ids`).
- `ems.attendance_session_header`'s `group_ids`/`subject_id`/`space_id`/`template_teacher_ids`/`study_ids` (new) converted from `sudo()`-laden compute methods to genuine `related=` fields; `level_id` is no longer related to the template (which has none) and is instead computed from `group_ids[:1].level_id`. `ems.attendance_session_line.study_id` is likewise now `study_ids`.
- The template form is reorganized into a 3x3 grid (color/teachers; studies/start date/end date; groups/space/subject) and gains chatter (`mail.thread`/`mail.activity.mixin`).
- Migration `migrations/18.0.0.22.0/{pre,post}-migrate.py`: renames the old `study_id` column out of the way before schema sync, then backfills `study_ids` on the template and cascades it down to every existing session header/line via SQL joins.
- Documented in `docs/en/developers/attendance/{attendance_template,attendance_schedule,attendance_session}.md`; cross-references in `docs/en/developers/curriculum/{study,level}.md` and `docs/en/developers/employees/working_schedule.md` updated to match.
- The archive-or-write decision behind the "Edit" button is now a shared method, `ems.attendance_mixin._write_or_new_version(vals)` (`models/shared/attendance_mixin.py`), reused by the schedule-sync pipeline instead of each reimplementing the same `has_sessions` check. `_archive_stale_schedule_sync`/`_write_schedule_sync` (shared by the live Schedule tab's own edits and the working-schedule import wizard) changed from unconditionally archiving and recreating **every** schedule line of a persisting template to a per-line match: an untouched line is left alone, a changed line with no real sessions is updated in place (same DB id), and a changed line that already has sessions is archived and replaced with a fresh one - the same rule a manual edit already follows, now applied uniformly regardless of which caller triggers the sync.

# Fixes:

## "The subject must be available in every one of the selected studies" error gave no way to tell which subject, study or template it was about:
- Developer feedback (2026-08-10, hit for real): *"He tenido un error... pero no sé a qué estudio se refiere ni dónde ha aparecido este problema."* `ems.attendance_template._check_subject_valid_for_all_studies` raised a fixed, generic sentence with no reference to the actual subject, the studies missing it, or which template (which group/teacher) triggered the check — unactionable on its own, especially when raised from a batch context (e.g. the working-schedule import wizard) with several templates involved.
- Fixed to name the subject, list exactly which of the template's selected studies don't teach it, and identify the template itself by its group(s) and teacher(s) — e.g. `"The subject 'Mathematics' is not available in the following selected studies: CFGM Study (template for group(s) DAM1A, teacher(s) Jane Doe)."` New regression test (`test_invalid_subject_for_study_error_identifies_subject_study_group_and_teacher`) asserts all four pieces of information are present in the message, plus a real Catalan translation verified functionally under a `lang='ca_ES'` context. `TestAttendanceTemplate` (28/28) green.

## "File conflicts" now catches a same-teacher double-booking across two different rooms within the same batch:
- Developer feedback (2026-08-10, hit against a real import): after manually assigning two DIFFERENT raw identifiers (two placeholder codes) to the SAME existing employee - exactly the "same person as" merge this session's earlier "Resolve teachers" work made possible - that one real teacher turned out to be double-booked at the same time in two different rooms. Neither "File conflicts" (room-based only) nor the existing DB-side `find_self_conflicts` (documented as "does not catch two overlapping entries for the same teacher within the single batch being submitted right now") ever checked for this - it surfaced as a raw, unworded `check_overlap` ValidationError at the final Import click instead of a resolvable line.
- New `self_conflict` kind: `_find_self_conflicts_in_batch()` pairs every two entries from DIFFERENT node_cache items both resolved to the SAME `employee_id` whose weekday/time overlap, regardless of room - added to "File conflicts" (`_build_internal_conflict_lines`) alongside the existing room-based pairs, excluding anything already caught there. Labeled "Same teacher, different room", only offers "Left prevails"/"Right prevails" (never "Confirm"/"Reassign rooms" - a room swap fixes nothing when the real problem is one teacher needed in two places at once), defaulting to "Left prevails".
- New browser tour (`ems_working_schedules_import_resolve_self_conflict`) + 5 new backend tests. `TestWorkingSchedulesImportWizard` (94/94) and `TestWorkingSchedulesImportWizardTour` (10/10, once the bulk-apply widget below also landed the same session) both green.

## "File conflicts"/"Existing schedule conflicts" grouped into cards, with a bulk-resolution dropdown per group:
- Developer feedback (2026-08-10, same real import as the self-conflict fix above, resolving dozens of conflicts by hand): *"me iría bien que estuvieran agrupadas por tipo (co-teaching, etc) y por 'left', y que cada grupo me permitiera escoger el resolution que se aplica al grupo entero. Si escojo ese resolution, se aplica a todos los desplegables (aunque luego yo cambie uno a mano). Para agrupar, quizás el formato tarjetas que hemos usado en el resumen final nos pueda venir bien."*
- Both screens' flat editable list replaced by a new custom widget (`ems_grouped_conflict_lines`, `static/src/js/backend/grouped_conflict_lines_field.js` + its own OWL template) that groups the same underlying records **client-side, no new RPC**: one Bootstrap card per conflict kind actually present (reusing the "Overall summary" screen's own card styling), and within each card, one bordered block per distinct left-hand entry ("por 'left'"). Each block gets its own small dropdown ("— apply to all —") - picking a resolution there writes it onto every row in that block at once via `record.update()`, then resets itself; every row keeps its own independently-editable resolution dropdown right below for a one-off override afterward ("aunque luego yo cambie uno a mano").
- Room pickers (`left_space_id`/`right_space_id`, shown once a row's resolution is "Reassign rooms") use Odoo's generic `AutoComplete` component directly (the same one the standard Many2one widget itself is built on), driven by a plain `ems.space` `name_search` call - not the generic `Field` component, whose Many2one only renders as editable once a record is "in edition" (confirmed empirically via a real screenshot: forcing edit-mode on every row still rendered the picker as inert text).
- The old `ems_conflict_resolution` selection-field widget and its own `working_schedules_import_wizard.css` (row-color decorations, column width fixes) are both deleted outright, superseded by the new widget's own per-kind option filtering and by which card a row appears under. 8 now-orphaned/stale `.po` blocks cleaned up in both `ca_ES.po`/`es_ES.po` (6 fully removed, one trimmed of a stale reference it shared with an unrelated field); every new JS-side label got its own `#. odoo-javascript`-tagged translation, reusing the same already-proven Catalan/Spanish text as the pre-existing Python/view-arch entries for it.
- New tour (`ems_working_schedules_import_bulk_apply_resolution`, three groups sharing one classroom so the anchor's own entry collides with both others, forming a 2-row group) proves the bulk selector actually resolves every row under it at once - verified functionally via "Continue" only enabling once every row (both bulk-applied groups) has a valid resolution, not by inspecting each row's own dropdown value directly (unreliable for a native `<select>`, whose current value is a DOM property, not an HTML attribute a tour selector could match). The 3 pre-existing tours that exercise either screen (`resolve_internal_conflict`/`resolve_db_conflict`/`resolve_self_conflict`) all needed their own selectors rewritten (`.o_data_cell`/`.o_data_row` no longer exist under the new rendering) - `TestWorkingSchedulesImportWizardTour` now 10/10.
- Dev doc (`docs/en/developers/employees/working_schedule.md`) and all 3 admin-doc languages updated for both this and the self-conflict fix together (deliberately not deferred any further - both changes are now confirmed-final and tested); one new screenshot (`working-schedules-import-03-grouped-conflicts.png`, replacing the stale flat-list one) cropped from an already-clean tour screenshot (only fictitious "Tour..." seed data ever visible).
- **Same-day refinement, after the developer actually saw the first version rendered for real:** *"veo una targeta por vila, no hay agrupación ninguna. Creo que la forma más práctica de agrupar es por docente y materia, ignorando el resto de valores."* Grouping by the full `left_label` (teacher+subject+group+weekday+time) turned out too specific in practice - two conflicts almost never share the exact same group/weekday/time too, so every sub-group ended up with exactly one row. New `left_group_key` field (Char, `conflict_mixin`, via a new shared `_entry_group_key()` helper - just `"teacher — subject"`, no group/weekday/time) is a deliberately coarser identity used **only** for grouping; `left_label`/`right_label` themselves stay the full, specific per-row description.
- **Second same-day request:** *"tampoco queda claro quien es left... quien es right. Habría que aclararlo, o cambiar left/right por otra cosa."* The bare words "left"/"right" are gone from the UI entirely (the underlying field names are unchanged) - "File conflicts" (both sides are file entries, no real asymmetry) now joins the two full descriptions with a plain `"vs."`; "Existing schedule conflicts" (genuinely asymmetric) prefixes each side `"File: ..."`/`"Database: ..."`, reusing the exact words the old list's own column headers already used.
- New `.po` entries for the 2 new JS-side labels ("File"/"Database", `#. odoo-javascript`-tagged) and the new field's own `field_description` ("Group key") in both languages. Visually re-verified via a real tour screenshot (temporarily forced, then reverted) before reporting this done - confirms both complaints are actually fixed, not just the code changed.
- `TestWorkingSchedulesImportWizard` (89/89) and `TestWorkingSchedulesImportWizardTour` (10/10) both re-confirmed green; pylint clean.
- **Two more same-day polish items, right after the developer tried the grouped view for real:**
  - *"El interior de las tarjetas queda poco alineado... los desplegables... quizás con una tabla sea sencillo... si la alineación coincide entre tarjetas, quedará más bonito."* New small CSS file (`grouped_conflict_lines_field.css`) puts every row (the sub-group's own header row included) on the SAME fixed-width CSS Grid template (`minmax(0, 1fr) 170px 150px 150px` - description/resolution/2 room pickers) - since it's one shared rule, every dropdown lines up not just within a sub-group but across every sub-group and every card.
  - *"Xavier Cruz — MP 0440: ... (GA1A) (GA1A, Monday 08:00-09:00)"* - the group name was showing up TWICE per row: once already baked into the cached entry `name` (added once its group is resolved), once again from `_entry_label`'s own trailing `(groups, weekday time)`. New `_entry_subject_label(entry)` resolves the subject's clean `"<acronym>: <name>"` straight from `ems.subject` instead of reusing the group-suffixed cached `name` - fixes the visible duplicate AND a related, not-yet-visible bug in `_entry_group_key` (the SAME cached `name` was also used there, meaning the "group by teacher+subject, ignoring group" grouping added earlier the same day didn't actually ignore group at all for any teacher whose own colliding entries span more than one group - the realistic, common case - only appeared to work in the earlier verification tour because that fixture's own "left" entry happened to be literally the same one reused across every pair).
  - Re-verified visually (temporarily forced screenshot, reverted before the final run) - confirms both fixes together: one merged parenthetical, dropdowns aligned in clean columns.
- Developer feedback (2026-08-10, found testing a real import): *"durante la importación... debería aparecer el loading modal que bloquea toda la ventana, el mismo que usamos durante la migración."* The wizard's own bespoke blocking controller (`working_schedules_import_wizard_form_controller.js`) blocked in `beforeExecuteActionButton` and unblocked immediately after, in a `finally` - covering only the record *save* that precedes a button's own server RPC, on the assumption that "the button's own server method runs after the save and is comparatively cheap." True for "Continue" (the slow XML parsing happens *during* that save), but false for "Import": `import_planner_data()`/`_apply_import()` IS the slow part, and it runs entirely *after* the save - the overlay was disappearing right as the real write was about to start.
- Rewritten to reuse `blockingActionFormView()` (`blocking_action_form.js`) - the exact same shared factory the course transition wizard already uses - instead of a separate, subtly-different implementation. It only unblocks in `afterExecuteActionButton`, called after the button's ENTIRE click (save + its own RPC + resulting action), covering whichever phase turns out to be slow instead of assuming which. Same two messages as before ("Processing, please wait..."/"Importing the schedule, please wait..."), no new translatable strings.
- No new tour assertion for the overlay's own visual timing - no established, reliably non-flaky pattern exists in this codebase for asserting a transient full-window overlay in a tour (not even the course transition's own tour attempts it), and the fixtures every existing tour uses are small enough that the real import completes near-instantly either way. The existing 8 tours re-confirm the Continue/Import click path itself still completes correctly after the refactor.
- **Two more same-day fixes, right after the developer used the grouped-cards widget again on a real import:**
  - *"usas un guion largo — que no me gusta. Usa un guion simple -"* - `_entry_label`/`_entry_group_key`/`_external_conflict_label`/`_conflict_lines`'s `"%(teacher)s — %(subject)s"`-shaped strings all changed from an em dash to a plain hyphen; the JS widget's own `rowText()` ("File: ... — Database: ...") had the same decorative em dash and got the same fix for consistency, even though it wasn't the specific line quoted. New standing style rule added to CLAUDE.md's "Coding standards" (a decorative em dash joining two short fields should be a plain hyphen; a genuine grammatical em dash is fine) - generalized per the developer's own follow-up ("Recuerda lo del guion simple... si es una decoración, no la uses"), not a one-off fix. New `.po` entries added for the 3 affected msgids in both languages (they turned out to have never been translated at all, dash or otherwise - a pre-existing gap, not something the dash change itself made stale).
  - *"He aplicado 'Left prevails' en 'file conflicts' pero el continue no se activa... que no sea un problema... con una paginación que ya no existe"* - confirmed exactly that, by reading Odoo's own `relational_model.js`/`static_list.js`: an x2many `<list>` sub-arch with no explicit `limit` defaults to 80, and `StaticList` slices its own `records` to that "page" client-side even though the new grouped-cards widget renders no pager to reach anything past it - a batch with more than 80 conflict lines (or, found auditing the other two resolution screens the same way, more than 80 unresolved groups/teachers) silently left the excess lines both invisible AND permanently unresolved, while the server-side `continue_disabled` check correctly still saw them as blocking - "Continue" stayed disabled with no visible reason. Fixed by adding an explicit `limit="1000"` to all 4 affected `<list>` sub-archs (`group_line_ids`, `teacher_line_ids`, both conflict o2m fields) - confirmed `limit="0"` would have been actively dangerous (evaluates as JS-truthy, but Odoo's own arch parser reads the string as the integer `0`, meaning zero records rendered).
  - `TestWorkingSchedulesImportWizard` (90/90) and `TestWorkingSchedulesImportWizardTour` (10/10) both re-confirmed green after both fixes; pylint clean.

## Course transition wizard left archived templates' schedule lines active forever:
- Found while investigating what happens to teacher-side scheduling data across a course transition, in preparation for testing "Existing schedule conflicts". `course_transition_wizard.py`'s `_apply_cleanup` archived `ems.attendance_template` via a bare `write({'active': False})`, which bypasses `EmsAttendanceTemplate.action_archive()`'s own cascade to `attendance_schedule_ids`. A transitioned template went inactive, but its weekly schedule lines stayed `active=True` forever - invisible in the template's own (archived) form, but still matched by `classify_external_conflicts`/`find_self_conflicts`, silently contradicting those methods' own docstring assumption that a transitioned study has nothing active left to reconcile against.
- Fixed by switching to `action_archive()`. New regression test (`test_apply_archives_the_schedule_lines_of_an_archived_template`) creates a template with a real schedule line - the two pre-existing template-archiving tests never caught this because their own fixture creates templates with zero schedule lines.
- First of an 8-phase plan (`plans/course_transition_teacher_schedule_archival.md`) covering how the wizard should also archive affected teachers' `resource.calendar`/`resource.calendar.attendance` and roll them onto a new per-course calendar - the remaining phases are tracked separately, implemented one at a time.

## Course transition left real session headers stranded active, because room drift broke the calendar→schedule matching:
- Found auditing real dev data before a batch import (2026-08-10, developer request): 4 `ems.attendance_session_header` rows sat active despite their own schedule line/template already being correctly archived. Traced to `ems.attendance_mixin.find_schedule_lines_for_slot` (used by `course_transition_wizard._apply_calendar_archival()` to find which schedule line a teacher's calendar block backs) matching on room as well as weekday/time - a teacher can freely change the room while taking attendance for an unplanned reason (e.g. an unscheduled workshop), so the calendar block's own room had drifted from the schedule line's authoritative one, silently breaking the match.
- Renamed to `find_schedule_lines_for_teaching`, now matched by teacher + subject + ANY group overlap + weekday/time - room dropped entirely, per developer instruction ("lo que manda es el calendario... el aula no deberíamos usarla para las búsquedas"). Every matching line is now returned, not just the first, so a stale leftover alongside a newer one both get processed.
- **Second, orphaned-line case added the same day** ("lo que manda es el calendario"): a migrating teacher's own still-active line whose (subject, group, weekday, time) is no longer backed by ANY of their current calendar blocks at all - not just the specific block that made them "migrating" - now counts as departed too, via the same `_teacher_has_active_block()` predicate (also fixed to check subject+group, not just weekday/time) reused for both "is a remaining co-teacher still supported" and this new "is a departing teacher's own line still supported" check. Real scenario this closes: a teacher edits their calendar by hand, bypassing the normal sync, leaving an old line the direct block-match would never find.
- New/adapted tests in `tests/test_attendance_schedule.py` (renamed `find_schedule_lines_for_teaching` suite, including a room-drift regression and an inactive-leftover-plus-active-line case) and `tests/test_course_transition.py` (a dedicated orphaned-line test; the two pre-existing "decision 3/4" tests needed their shared `_calendar_block()` helper to also set a `subject_id`, since matching now requires it). `TestAttendanceScheduleLogic` (15/15) and `TestCourseTransition` (117/117) both green.
- **A bigger structural fix for this same "inferred link" problem - a real FK from `resource.calendar.attendance` straight to `ems.attendance_schedule`, replacing content-matching entirely - was proposed by the developer the same day and written up in `plans/calendar_driven_attendance_templates.md` for later, rather than attempted immediately: it also reshuffles where `student_ids` lives (template → schedule) and who's allowed to create/archive a template directly, a bigger redesign than this fix's own scope.**
- **"Attendance reports" pivot/graph showed every course's history forever, with no current-course scoping at all** (2026-08-10, developer feedback: *"debe mostrar la información del curso actual. Una vez transicionamos de curso, debería estar en blanco"*): `ems.attendance_session_line` deliberately has no `active` field of its own (it's documented "statistical data... should be unaltered"), and the report's own action had no domain at all beyond the existing teacher-scoping for non-privileged roles. Fixed by adding `[('attendance_session_id.active', '=', True)]` - the line's own session always has a real `active` field, and the course transition wizard already archives every outgoing session, so this is exactly "current course only" with no new field needed. New tests confirm both the admin and teacher-scoped domain now exclude a line whose session is archived.
- **Third gap, found re-running a real transition after the above two fixes landed:** the two checks above only ever look at a teacher who's currently "migrating" (an active calendar block, or an active line to compare one against) - a teacher whose *entire* calendar was already fully archived in an *earlier* run (zero active blocks left, so they never enter `affected_teachers` at all this time either) can still have a stale line from back then whose session catch-up was simply never reached by either check. Real case: exactly the same David Delgado data from the first audit - his template/line were already archived, his whole 2025-2026 calendar already fully rolled over, yet the 4 stray sessions survived a second transition run too. Added a third, fully unscoped catch-up in `_apply_calendar_archival()`: any already-archived line anywhere with still-active sessions gets those sessions archived, regardless of this run's own `affected_teachers`/`study_ids` - a plain, unconditional data-integrity invariant ("an archived line's sessions are also archived") rather than something tied to any specific migration's own scope. New test covering exactly this "not migrating at all, already archived earlier" shape. `TestCourseTransition` now 118/118.

## Course transition never cleaned up attendance justifications or "daily issues" for a student already stranded by an earlier run:
- Same 2026-08-10 audit: 2 `ems.attendance_justification` and 27 "daily issue" rows (10 `ems.attendance_issue_tutor` + 17 `ems.attendance_issue_status`) sat active, all referencing sessions that were themselves already correctly archived. `res.partner._ems_clear_operational_records()` (the existing cleanup for these two model families) only runs for students still in `_scope_students()` at run time - captured by *current* group membership - so a student already detached from any group (stranded by an earlier run's `_apply_detach_unplaced()`, or a reinforcement-group student never captured by `main_group_id` at all) fell outside that scope forever.
- New `_apply_calendar_archival()`-adjacent step, `_apply_attendance_records_archival()` (step 7c), keyed on the *session's* own archived state instead of student scope - archives (never deletes, unlike the existing D6 mechanism) a justification once every one of its linked session lines is archived, and an issue status/student/tutor chain the same way, walking up once each parent is left with no active children.
- New tests in `tests/test_course_transition.py` covering both the positive (session archived → issue/justification archived) and negative (session untouched → left alone) cases. `TestCourseTransition` now 117/117.
- **Two more gaps found re-running a real transition after the fix above landed:**
  - **`ems.attendance_justification.attendance_session_line_ids` is not the real link.** That M2M is a form-editing convenience ("Many2many needed in order to update values", per its own code comment), kept in sync with the real link only when a justification is created/edited *through the UI*. The real, authoritative link is `ems.attendance_session_line.attendance_justification_id`/`attendance_prevision_id` (a Many2one FROM the line TO the justification), set automatically by session auto-creation without ever syncing back onto the justification's own M2M - so a real, dated-in-the-past justification can show zero entries on that M2M while still being linked from the line side, or never having any session at all once the course it covered has fully ended. Fixed to check both relations, and to treat zero lines (of either kind) as archivable too, once the justification's own `end_date` has already passed - protects a genuine future "prevision" from being swept up just because no session has been created for it yet.
  - **Ordering conflict with the existing D6 deletion mechanism**: the new archival step ran *before* `students._ems_clear_operational_records()` (the pre-existing method that DELETES an issue_student/_tutor for a student actually leaving in this run) - archiving an empty issue_student first made it invisible to that deletion's own active-only search, so it survived archived instead of gone. Moved the new step to run *last* in `_apply_cleanup()`, after that deletion, so it only ever catches what the deletion was never going to touch anyway.
  - Also broadened the issue_student/_tutor check to look at every record's *current* children directly, not just the ones this call's own status-archival just emptied - an issue_student/_tutor with zero children *from the start* (never had any) previously never got re-checked at all.
  - New/adapted tests: a past-dated, zero-lines justification gets archived; a future one doesn't; a zero-children issue for a stranded student gets archived; the pre-existing D6 deletion test now also pins down the reordering (its own docstring updated to explain why). `TestCourseTransition` now 121/121.
- **Deletion vs. archiving policy across the whole course-transition cleanup was never actually audited** - raised by the developer while confirming this session's own new code only ever archives, never deletes (a pre-existing, unrelated method, `_ems_clear_operational_records()`, is what deletes issue_student/_tutor + several other models for a student actually leaving). Written up as `plans/course_transition_delete_vs_archive_audit.md` for a future session: per-model, is deleting it safe because the info survives in `ems.student.year_record`, or does it lose real detail nothing else keeps? Also flags a genuine inconsistency worth resolving on its own: the exact same two models (`attendance_issue_student`/`_tutor`) are deleted for an in-scope student but archived for one outside scope, with no deliberate reasoning behind which.

## `test_data_main_cat_prefix` was red on a full, unscoped `./test.sh` run:
- `data/main/hr.departure.reason.csv` (added earlier for the reason-aware ribbon work below) deliberately overrides two native `hr.departure.reason` records to add a `color`, keeping their native `hr.` id prefix - a legitimate, documented exception (CLAUDE.md's "Data folder conventions"), but the test's own allowlist of accepted native-override ids was never updated to include them, so it flagged them as a naming violation. Fixed by adding `hr.departure_retired`/`hr.departure_resigned` to `ALLOWED_NATIVE_OVERRIDE_IDS` in `tests/test_data_main_cat_prefix.py`, matching the existing `hr.job_*` entries. Found running the full suite as the final gate for the `attendance_template` multi-study work, unrelated to that change itself.

## Every reason-aware ribbon showed the default red, never the actual color:
- `archived_reason_color` (Alumni's green, Withdrawal's orange, a departure reason's own color...) was only ever referenced through the `ems_archived_reason_ribbon` widget's own `color_field` option, never as its own declared `<field/>` in the view - and, unlike Many2many field widgets, this field registry's `relatedFields` mechanism turns out to only auto-fetch for x2many/many2one_reference field types in this Odoo version (confirmed by reading `web/static/src/views/fields/field.js`), never for a plain `Char` like this one. The color value was therefore never actually reaching the client, and the widget's own default-red fallback showed every time, regardless of the real color.
- Fixed by explicitly declaring `<field name="archived_reason_color" invisible="1"/>` alongside the ribbon field in all four views (`contact/{form,kanban}.xml`, `employee/{form,kanban}.xml`). New tour steps assert the actual rendered color (not just the text) to catch this specific regression going forward.

## Working-schedule import wizard showed a generic error popup instead of the usual red banner:
- An unresolvable group name or subject code in an uploaded schedule file raised uncaught from inside the onchange preview, so Odoo displayed it as a blocking modal dialog instead of the same in-form banner (with the Import button disabled) used for every other validation problem in this wizard (unknown teacher e-mail, missing classroom...).
- Both onchange handlers (`_onchange_file`, `_onchange_attachment_ids`) now catch that error and fold it into the same in-form banner, consistently with the rest of the wizard.
- That banner (and every other blocking, per-item problem: unknown e-mail, unresolved group/subject, missing classroom) now renders as an intro sentence + a real bullet list (one line per problem), matching the existing "these teachers already have a schedule" warning style, instead of one long comma-separated sentence.

## Import button could be clicked before file validation had actually finished:
- Attaching a file made the button visible/enabled immediately (it was only hidden once a problem was confirmed), so clicking fast enough could trigger the import before the background validation RPC returned.
- The button now starts hidden and is only ever shown once the same onchange positively confirms the file has no blocking problems - there is no window where it can render as clickable before that.

## Pending-identification info banner truncated a not-yet-hired teacher's full name to its first word:
- Both the blue "pending identification" banner and the actual pending-employee creation identified a not-yet-known teacher by naively taking the first whitespace-separated token of the planner row's raw `name` attribute, assuming the shape was always `"<code-or-email> <discardable label>"`. That assumption holds for a short placeholder code (`"X1"`) but not when the planner instead puts the person's own real, multi-word name in that field (e.g. `"Fulanito Menganito"`) - the old code silently kept only `"Fulanito"`.
- Fixed with a proper check (`_teacher_identifier`, `models/employees/working_schedule.py`): search the whole value for an actual e-mail pattern; if none is found, keep the entire (stripped) value as the identifier instead of just its first token. Applied uniformly everywhere a teacher node's `name` is parsed (`_onchange_file`, `_onchange_attachment_ids`, `create()`).
- The blue banner (`info_message` → `info_html`) now renders as a real bullet list too, matching the other banners in this wizard, instead of one comma-separated sentence.
- All four of this wizard's list banners (blocking issues, pending-identification, overridden teachers, external conflicts) now lay out their bullet list in up to 3 CSS columns (`ems_wizard_bullet_list` in `ems.css`) instead of one long single-column list, since these are short flat items and a long list was wasting the dialog's horizontal space - collapses to 1 column on narrow screens.
- Fixed a follow-up regression from the 3-column layout above (reported 2026-08-01): a single long line (e.g. one unknown-e-mail error, or one conflict line) had its own wrapped text visually split across all 3 columns instead of staying together in the first one - CSS multi-column's default fragmentation behaviour. Fixed with `break-inside: avoid` on each list item; verified in a real browser via a new tour assertion (bounding-box width check) rather than trusting the CSS fix by reasoning alone.

## A not-yet-identified teacher's unresolved group/subject showed a generic error popup on Import click, not the red banner:
- The onchange preview never actually parsed a pending-code teacher's schedule content (subject/group resolution) - it only noted the code as "pending" and moved on. An unresolvable group acronym (or subject code) in that same row therefore passed the preview silently and only surfaced later, as an uncaught `ValidationError`, when actually clicking Import (`create()`) - Odoo's generic error dialog instead of the wizard's own red "blocking issues" banner.
- Fixed: `_onchange_attachment_ids` now parses every teacher node's schedule content up front regardless of whether its code/e-mail resolves to a real employee yet, so an unresolved group/subject is caught and listed as a blocking issue immediately, hiding the Import button - matching the exact same treatment a known teacher's unresolved group/subject already got.

## "Import" click showed no loading feedback either, same complaint as the file upload:
- Clicking **Import** implicitly saves the wizard first - that save *is* the slow `create()` override (parses the whole file, writes the schedule), with the button's own server method running trivially after it. Same "looks hung" complaint as the file-upload spinner, just on the other button.
- New `EmsWorkingSchedulesImportWizardFormController` (`static/src/js/backend/working_schedules_import_wizard_form_controller.js`, `js_class="ems_working_schedules_import_wizard_form"`) wraps that save with the same `ui.block()`/`.unblock()` overlay used for the file upload, scoped to the `import_planner_data` button only, via Odoo's own `beforeExecuteActionButton` hook (unblocking in a `finally` so a save failure - e.g. that same unresolved-group error - doesn't leave the overlay stuck).

## Working-schedule import wizard looked hung while a file was being processed:
- Attaching a file to either upload field (the per-employee scoped `file` or the general cog-menu's `attachment_ids`) triggers a server-side onchange that parses the whole planner XML - slow enough, with zero visual feedback, to look hung.
- Both fields now use new thin widget subclasses (`ems_blocking_binary`/`ems_blocking_many2many_binary`, `static/src/js/backend/working_schedule_import_blocking_upload.js`) that wrap the upload with Odoo's own `ui.block()`/`.unblock()` overlay (spinner + "Reading and validating the schedule file, please wait..." message) - the same native mechanism Odoo already uses for long button actions, not a bespoke spinner. See `docs/en/developers/employees/working_schedule.md`.

## "Pending identification" kanban badge showed on every teacher, not only pending ones:
- The badge's `t-if` read `record.pending_identification.value` - for a kanban record, `.value` is the *formatted* display value (for a Boolean field, Odoo's formatter returns an HTML checkbox markup string, which is always truthy regardless of true/false); `.raw_value` is the actual boolean and is what a conditional must use. Confirmed by reading Odoo's own `getFormattedRecord`/`formatBoolean` and by reproducing the bug live via a browser tour (a non-pending "confirmed" teacher's card also showed the badge).
- Fixed to `record.pending_identification.raw_value`, matching the `.raw_value` convention already used throughout this same view (and its `hr.hr_kanban_view_employees` parent). The pre-existing `tutorships` badge's `t-if` had the same `.value`/`.raw_value` mix-up (harmless in practice only because Char's formatter happens to return the same string) - fixed for consistency while in the area.
- Also moved the badge so it always renders on its own line directly below the employee's name (previously anchored after `job_title`, so it could end up sharing a line with the job title text when one was set).

## `devel.sh` left `hr.employee.work_email` stale after replacing real e-mails:
- `work_email` is a stored compute of `work_contact_id.email` (native Odoo); the script's raw SQL `UPDATE` on `res_partner.email` bypasses the ORM, so that cached copy on `hr.employee` never got refreshed - teachers kept showing their original (real-looking) e-mail even after the script ran.
- `devel.sh` now also refreshes `hr_employee.work_email` from the now-updated `res_partner.email` right after replacing it.

## Archiving a group with active students now asks for a single, clear confirmation:
- Archiving is always allowed and never removes/unenrolls anyone (a group's member list doesn't live on the group itself), but silently archiving a group people are still actively using could be confusing. Archiving one that still has active `main_student_ids`/`reinforcement_student_ids` now shows a clear 4-paragraph confirmation explaining exactly that (no student is removed, the group just stops showing in default views, and the end-of-course transition will still clear it if that's the reason), with **Proceed**/**Cancel** actions, instead of archiving silently.
- Declining leaves the group completely untouched.
- Exactly one dialog shows, not two or three: earlier iterations first fixed a redundant double-confirmation (Odoo's own generic "are you sure?" followed by our own), then fixed the remaining dialog's cosmetics (a fixed "Odoo Warning" title and a "Close" button that couldn't be relabeled - both inherent to Odoo's generic `RedirectWarning` dialog templates). The final design has `EmsGroupFormController`/`EmsGroupListController` (`static/src/js/backend/group_{form,list}_controller.js`) call a new `get_archive_confirmation_message()` RPC *before* ever attempting the archive, showing their own properly-titled `ConfirmationDialog` only when needed - Odoo's generic dialogs are never shown at all on this path. `write()`'s original guard remains as a safety net for any non-UI caller (a direct ORM call, an import script).
- `views/community/group/{form,list}.xml` now declare the `active` field (invisible) - discovered empirically that Odoo's Archive/Unarchive Action-menu item only appears on a view that declares this field itself, not just on the model.
- The group's form now shows the standard Odoo "Archived" ribbon when inactive (same `web_ribbon`/`invisible="active"` pattern used natively across Odoo core), so an archived group is visible at a glance, not just via the Action menu's label.

## "Archived" ribbon rolled out across every archivable model:
- Following the same standard Odoo pattern just added to `ems.group`, the "Archived" ribbon (`web_ribbon`, `invisible="active"`) now also shows on: `hr.employee`, `hr.department`, `ems.strike_reason`, `ems.attendance_status`, `ems.non_teaching_type`, `ems.strike`, `ems.attendance_correction`, `ems.attendance_template`, `ems.attendance_justification`, `ems.attendance_schedule`, `ems.attendance_session_header`, `ems.enrollment`, `ems.teaching`, `ems.grade_session`, `ems.notice`, `ems.limesurvey_header`, `ems.limesurvey_block`, `ems.limesurvey_recipient`.
- `res.partner` already had the native "Archived" ribbon (`base.view_partner_form`); for a **student** specifically it now says **"Alumni"** or **"Withdrawal"** instead (matching `contact_type`'s own selection labels) - clearer than a generic "Archived", and consistent with how the rest of the app already distinguishes the two. Other archived partners (family, provider, applicant) keep the native generic ribbon unchanged.

## Groups can now be archived and reactivated instead of duplicated:
- `ems.group` gains the standard Odoo `active` field (default `True`) — a group not running this course but that may come back in a future one (a cycle skipping a year, a shift suspended temporarily...) can now be archived via the Action menu (Archive/Unarchive), keeping its history (tutor, classroom, past students/schedule) instead of losing it to a delete-and-recreate cycle.
- Creating (or renaming, via `course`/`acronym`/`study_id`/`group_type`/`external_id`) a group into a name that matches an already-archived group no longer silently creates a duplicate: it raises a dialog offering a one-click "Reactivate" action (an `ir.actions.server` calling the new `action_reactivate()`) that reactivates the existing group and opens it, instead. Declining leaves everything untouched — the attempted create/rename is fully rolled back via a savepoint, nothing is created or renamed.
- Admin docs (`docs/{en,ca,es}/admin/groups.md`) and the technical reference (`docs/en/developers/contacts/group.md`) updated; new tests in `test_group.py` + a new browser tour (`ems_group_reactivate_archived_duplicate`) covering both the accept and decline paths.

# Internal changes:

## `resource.calendar.attendance` gains an `active` field (phase 2 of the course-transition archival plan):
- Core Odoo's model has none. Added so a later phase can archive a teacher's migrating calendar blocks at course transition instead of `unlink()`-ing them, which would destroy the exact history a "who taught what, which course" query needs (`plans/course_transition_teacher_schedule_archival.md`). All 1436 existing rows auto-backfilled to `active=True` by Odoo's own new-column handling. Nothing writes `active=False` here yet - that's a later phase.
- New test: `test_attendance_row_active_defaults_true_and_can_be_archived` (`tests/test_working_schedule.py`).
- **Explicitly confirmed, and pinned down with regression tests, that archiving an `ems.attendance_schedule` line must never cascade to its `ems.attendance_session_header` records, in either direction**: once a line `has_sessions`, its logistics fields are readonly, so a routine correction (e.g. fixing its room) silently archives the old line and clones a new version (`action_new_version()`) as a side effect of what looks like a normal in-place edit - if that cascaded to sessions, every such correction would wipe the line's attendance history from a teacher's default view, the opposite of the intent. New tests: `test_action_archive_does_not_cascade_to_sessions`, `test_action_archive_on_template_does_not_cascade_to_sessions` (`tests/test_attendance_template.py`).

## `resource.calendar` gains `employee_id`/`course_id` (phase 3 of the course-transition archival plan):
- A teacher's personal calendar is now a permanent, queryable historical record of "who taught, in which course" on its own terms - `hr.employee.resource_calendar_id` only ever points at an employee's *current* calendar, so it stops working as a history source the moment a future phase supersedes it (e.g. at a course transition). `name` stays a plain stored `Char` (not computed) but is now auto-derived from these two fields at creation (`resource.calendar.create()`) whenever the caller doesn't pass an explicit one, matching the long-standing `"<teacher> (<course>)"` convention from a single source of truth instead of every caller building the string by hand.
- `ems_employee.create()`/`write()` updated accordingly: `create()` now passes `employee_id`/`course_id` to the new calendar instead of pre-building its name; renaming an employee now calls the calendar's own new `_refresh_personal_name()` instead of a removed `_personal_calendar_name()` helper.
- `get_employee()` now prefers the stored `employee_id`, falling back to the pre-existing reverse search (`hr.employee.search on resource_calendar_id`) for a calendar predating this field - keeps working once a calendar is no longer any employee's *current* one.
- Both fields are blank on every calendar created before this phase; not yet backfilled by a migration (pending a decision on bumping the manifest version for it). Also deliberately not yet touched: the XML batch importer's own calendar-minting-by-name-search logic (`_write_teacher_schedule`) - still creates calendars the old way for now, a later phase ("the wizard takes over calendar creation") replaces it.
- New tests: `test_create_teacher_calendar_gets_employee_id_and_course_id`, `test_get_employee_prefers_stored_employee_id_over_a_superseded_link` (`tests/test_employee_schedule_lifecycle.py`); `test_create_derives_name_from_employee_and_course`, `test_create_derives_name_from_employee_alone_without_a_course`, `test_create_respects_an_explicit_name_over_the_derived_one` (`tests/test_working_schedule.py`).
- Migration added the same day (`migrations/18.0.0.22.0/post-migrate.py::_backfill_calendar_employee_and_course`, same version - no manifest bump, per developer instruction): backfills both fields on every pre-existing `resource.calendar` from the same signal `get_employee()`'s reverse-search fallback already relies on (an employee whose *current* `resource_calendar_id` still points at that calendar), `course_id` set to that employee's company's current course. Verified against this dev DB's actual pre-upgrade state (temporarily rolled back `ir_module_module.latest_version` to force a real migration re-run, per CLAUDE.md's migration-testing convention): 55 of 56 eligible calendars backfilled; the 1 left blank is the pre-existing orphaned-calendar cardinality bug (same plan) - no employee currently points at it, so it's unreachable via this signal, harmless since nothing reads these fields yet.

## New shared lookup: `ems.attendance_mixin.find_schedule_lines_for_slot` (phase 4 of the course-transition archival plan):
- `resource.calendar.attendance` (a teacher's weekly calendar block) and `ems.attendance_schedule` (the recurring class-session line) have no direct FK - only an inferred `(weekday, hour_from/start_time, hour_to/end_time)` match. Added a standalone reverse lookup - given a teacher + weekday + start/end time (+ optional room), returns every currently active schedule line for that teacher overlapping that slot - after a full audit confirmed the several existing near-identical (weekday, time)-matching methods (`classify_external_conflicts`, `find_self_conflicts`...) are each too tied to their own caller's extra filters to reuse directly.
- Purely additive this phase - no caller wired in yet (that's phase 5, the course-transition wizard's own archival cascade, which needs this to find which schedule line(s) back a calendar block being archived at transition).
- New tests in `tests/test_attendance_schedule.py`: `test_find_schedule_lines_for_slot_finds_the_matching_line`, `test_find_schedule_lines_for_slot_filters_by_teacher`, `test_find_schedule_lines_for_slot_requires_time_overlap`, `test_find_schedule_lines_for_slot_optional_space_filter_excludes_a_different_room`.

## Course transition preview now counts affected teacher calendar blocks (phase 5b of the course-transition archival plan, read-only so far):
- New `ems.course_transition_wizard._migrating_calendar_blocks()`: every active `resource.calendar.attendance` row, on any teacher's real personal calendar, whose `group_ids` belongs to a study in the run's scope - the calendar-side mirror of the existing `_scope_templates()` domain, read directly from the calendar block rather than trusted purely via the template it backs (a teacher can build their own schedule bypassing the normal sync, per the plan's own decisions 3/4).
- New `calendar_block_count` field, shown in the preview alongside the existing `template_count`, plus a matching warning line ("N teacher calendar block(s) will be archived.") - counted only, nothing archived yet. The actual archival cascade is a later subphase of the same plan.
- New tests in `tests/test_course_transition.py`: `test_migrating_calendar_blocks_finds_a_block_for_a_group_in_scope`, `test_migrating_calendar_blocks_excludes_a_group_out_of_scope`, `test_migrating_calendar_blocks_excludes_framework_calendars`, `test_preview_sets_calendar_block_count`.
- i18n for the new field label/warning string deferred to this plan's own eventual Close step (all 8 phases), matching how phases 1-4's more internal changes were handled - not yet added to `.po` files.

## Course transition now actually archives teacher calendar blocks and their schedule lines (phase 5c of the course-transition archival plan):
- New `ems.course_transition_wizard._apply_calendar_archival()` (step 7a, right after the existing template-archival step): archives every migrating calendar block found by phase 5b's query, then finds the matching `ems.attendance_schedule` line(s) via the phase-4 lookup and either archives the line + its sessions (when the underlying template already got archived by the existing, unconditional per-study mechanism - the common case, where this call just catches up the sessions that mechanism's own cascade deliberately skips) or, for a line the template-side never touched (a calendar drifted to reference an in-scope group its own template doesn't), checks whether another teacher's calendar still has an active block for that exact slot before deciding to keep the line alive (dropping only the departing teacher from `teacher_ids`) or archive it outright.
- Found and fixed a genuine ordering bug while testing this against real fixtures: the initial version searched only *active* schedule lines, so a line already archived moments earlier by the template-cascade was silently skipped - meaning its sessions never got explicitly archived at all, the exact gap this phase exists to close. Fixed by searching with `active_test=False` and branching on the line's own current `active` state.
- New tests in `tests/test_course_transition.py`: `test_apply_archives_the_migrating_calendar_block`, `test_apply_leaves_an_out_of_scope_calendar_block_active`, `test_apply_archives_the_matching_schedule_line_and_its_sessions`, `test_apply_archives_the_template_once_its_only_line_is_archived`, `test_apply_removes_only_the_departing_teacher_when_another_still_has_an_active_block`, `test_apply_creates_a_new_template_version_when_the_departing_teacher_has_sessions`, `test_apply_with_no_migrating_calendar_blocks_is_a_no_op`. `TestCourseTransition` (104 tests) green.
- **Corrected same day (developer caught it in review): removing a departing co-teacher must never write `teacher_ids` directly.** The first version dropped the departing teacher via a raw `line.attendance_template_id.teacher_ids -= departing` write - `teacher_ids` is a locked identity field once the template `has_sessions` (see `attendance_template.md`'s "Identity fields, locking, and the 'Edit' button"), so this would have retroactively rewritten every already-taken session's own `template_teacher_ids` (a `related` field), corrupting who *actually* co-taught each past session. Fixed by deciding **once per template** (not once per line/teacher) with the *full* set of departing teachers found across all of that template's migrating lines, then going through `_write_or_new_version()` (`ems.attendance_mixin`) - the exact same mechanism the "Edit" button already uses: writes in place only if there's no session history yet, or archives the original (leaving its `teacher_ids`/sessions historically untouched) and clones a corrected version otherwise. Deciding per template also means a template every one of whose co-teachers departs in the same run is simply archived outright, no needless clone ever created.

## Found and fixed two real, pre-existing bugs in `ems.attendance_mixin`/`action_new_version()` (shared by the "Edit" button and now phase 5c above):
- **`ems.attendance_template.attendance_schedule_ids` was missing an explicit `copy=True`.** Odoo's `One2many` field defaults to `copy=False` - without the override, `action_new_version()`'s clone silently ended up with **zero schedule lines**, for any template with real session history, since it was first built. Undetected because the only existing test never asserted the clone's lines were actually there.
- **The clone's follow-up `.action_unarchive()` needed `with_context(active_test=False)`.** Even with `copy=True` fixed, the freshly-copied (still-inactive) lines are excluded by a plain O2M read's default active filtering, so the unarchive call was silently a no-op on an empty recordset.
- Also disabled the generic Odoo "Duplicate" action on `ems.attendance_template`'s views (`duplicate="0"`) - now that lines genuinely copy, a plain Duplicate (which never archives the original first, unlike `action_new_version()`) would immediately self-collide via `check_overlap`.
- New regression assertions added to the existing `test_action_new_version_template_archives_and_clones_whole_template` (`tests/test_attendance_template.py`): the clone's `attendance_schedule_ids` now must actually exist, be active, and match the original line's weekday. `TestAttendanceTemplate` (25 tests) green.

## The transition wizard now rolls a teacher's calendar onto the next course once teaching empties out (phases 6-7 of the course-transition archival plan):
- New `ems.course_transition_wizard._apply_calendar_rollover(teachers)` (step 7b, right after phase 5c's archival step, using its returned affected-teacher set): for each teacher whose calendar now has zero remaining *active teaching* blocks (a non-teaching commitment like a guard duty never counts, so it never blocks the rollover), rolls them onto a calendar for `target_course_id` - reactivating one already made for that exact `(teacher, course)` pair if a previous transition cycle already created and archived it, or creating a fresh one seeded from the outgoing calendar's own framework otherwise. The outgoing calendar is archived, never left orphaned.
- `resource.calendar._write_teacher_schedule` (the XML batch importer's own write path) simplified to just write onto `teacher.resource_calendar_id` directly - it used to search for (or create) a calendar by a `"<teacher> (<course>)"` name string, the exact mechanism that silently orphaned a teacher's previous calendar every time that string changed. Every teacher already has a calendar (auto-created at `hr.employee.create()` time); rolling it onto a new course is now the transition wizard's own job, not the importer's.
- New tests in `tests/test_course_transition.py`: `test_apply_rolls_a_teacher_over_to_a_fresh_calendar_once_teaching_empties_out`, `test_apply_keeps_the_calendar_when_teaching_remains`, `test_apply_rolls_over_even_if_only_non_teaching_entries_remain`, `test_apply_reactivates_an_existing_archived_calendar_for_the_target_course`, `test_apply_seeds_the_new_calendar_from_the_old_ones_framework`. `TestCourseTransition` (109 tests) and `TestWorkingSchedulesImportWizard` (69 tests, unaffected by the importer simplification) both green.

## Archived attendance templates/sessions/calendars are now findable again, and a teacher-calendar history browsing view (phase 8, last phase, of the course-transition archival plan):
- `ems.attendance_template`/`ems.attendance_session_header`'s own search views had no "Archived" filter at all - confirmed Odoo does not auto-add one for models with an `active` field (it's a per-view opt-in every search arch has to declare), so an archived record from any of this plan's earlier phases (or the pre-existing "Edit"/`action_new_version()` button) was simply unreachable from the list view's search bar. Both now have it; the session search view also gained a `session_teacher_id` field so it has anything searchable at all.
- New `views/community/working_schedules/search.xml` (inherits `resource.calendar`'s native search view, which already had "Archived"): adds `employee_id`/`course_id` as searchable fields and a **Course** group-by option, plus the same two fields as optional list columns - this is what actually exposes the "who taught, in which course" historical query the plan's earlier phases built `resource.calendar.employee_id`/`course_id` for.
- Confirmed (not fixed - it already worked) that the Schedule tab's grid widget still renders correctly for an archived employee whose calendar was never rolled over - extended `employee_archived_reason_tour.js`/`test_employee_archived_reason_tour.py` with a real attendance row + a Schedule-tab check.
- New tours: `ems_attendance_template_archived_filter`, `ems_attendance_session_archived_filter`, `ems_working_schedule_course_grouping` (`static/tests/tours/attendance_archived_filter_tour.js` + `tests/test_attendance_archived_filter_tour.py`).
- **i18n reconciled for the whole 8-phase plan**, closing the gap deferred since phase 5b: real Catalan/Spanish translations added for `resource.calendar.employee_id`/`course_id`, `ems.course_transition_wizard.calendar_block_count` (field label + its warning message), and the new/changed search-view strings - verified directly against `ir_model_fields` via `psql`, not just the `.po` diff.
- User docs updated in all three languages (`docs/{en,ca,es}/admin/course-transition.md`): the calendar rollover mentioned in step 3, plus a new note on browsing archived templates/calendars via the Archived filter and the Course group-by.
- `plans/course_transition_teacher_schedule_archival.md` deleted - all 8 phases are implemented; its design reasoning is now folded into `docs/en/developers/{attendance/attendance_template,attendance/attendance_session,employees/working_schedule,settings/course_transition_wizard}.md`.

## CLAUDE.md gains two standing rules (developer feedback 2026-08-06):
- Screenshots saved into `docs/assets/` must never expose real personal data from the background of a captured window - crop to the specific region needed (usually excludes background list content on its own), pixelate/blur if it can't be cropped out, and never publish a screenshot without actually looking at the result first.
- A general "optimize for quota" habit: deliberately decide per-task whether to batch verification (`./upgrade.sh`/`./test.sh` runs, screenshot capture loops, etc.) to the end of a chunk of work or check after every step, rather than defaulting to one habit regardless of the situation - the project's growing size/complexity makes this compound.

## Debounced the test-launch desktop notification (`plans/test_notification_debounce.md`, now implemented and removed):
- Several test launches in quick succession (iterating on a failing tour: upgrade → test → fix → upgrade → test...) used to fire one desktop notification each - flagged as annoying by the developer. `/root/.claude/hooks/ems-test-notify.sh` (a user-level file, not part of this repo) now debounces this specific notification to once per 60s (`DEBOUNCE_SECONDS`, tweakable), tracked via a small state file (`/root/.claude/hooks/.last-test-notify`, deliberately outside `/mnt/claude-notify/` since that directory is the trigger-file drop zone the host watcher consumes from). The other two notification types (task-done, waiting-on-you) are deliberately NOT debounced, per explicit developer request.
- `docs/en/developers/tooling/ai_agent_test_notifications.md` and `CLAUDE.md`'s trigger-1 description updated to match; verified with a scratch `NOTIFY_DIR`/`STATE_FILE`/short `DEBOUNCE_SECONDS` so testing the debounce logic itself never spammed the real bridge.

## Merged in the course-transition wizard (`353-add-course-transition-wizard-setup-next-course`):
- Resolved conflicts in `i18n/{ca,es}_ES.po` (reference-list merges, plus one genuine duplicate msgid split across two unrelated strings), `migrations/18.0.0.22.0/post-migrate.py` (combined both branches' new backfill functions into one `migrate()`), `models/contacts/contact.py` (kept the incoming branch's `_ems_enrollment_in_force()`-based authorization-flags fix over this branch's own stale current-course-based query), `models/enrollment/enrollment.py` and `tests/__init__.py`.
- While validating the merge, also found and fixed a pre-existing duplicate `msgid` ("Subject (all enrolled)") in both `.po` files, unrelated to this merge - two distinct source strings had ended up as separate blocks instead of one shared one.
- Fixed `tests/test_course_transition.py::test_transition_state_is_not_copied`, which hit `ems.study`'s `unique_code` constraint (added on this branch, absent on the merged-in one, so the two never interacted until now) via a bare `.copy()` call - passes an explicit `code` override now, since the test is about `transition_state`, not code uniqueness.
- Found (not fixed) a related, pre-existing gap while investigating that failure: six `curriculum` models have a unique `code` constraint but no `copy()` override, so Odoo's native "Duplicate" action would fail the same way for a real user - documented in `plans/curriculum_unique_code_duplicate_action.md`.

## Employee model:
- `hr.employee` gains `schedule_import_code` (Char) and computed `pending_identification` (Boolean), added to `ems_employee` in `models/employees/employee.py`.
- New `views/community/employee/search.xml` (this model had no EMS-owned search view before).

## Migration:
- `migrations/18.0.0.22.0/pre-migrate.py`: clears the stored `noupdate` flag on `hr.departure.reason`'s three native records (`hr.departure_fired/resigned/retired`) so `data/main/hr.departure.reason.csv`'s new `color` values can actually reach them on upgrade — confirmed empirically that they were silently ignored otherwise (native `hr` module data ships `noupdate="1"`).
- New `hr.departure.reason` record `ems.departure_reason_transfer` ("Transfer").

## New files:
- `models/employees/departure_reason.py` (`hr.departure.reason` `color` field), `views/community/employee/departure_reason.xml` (color widget on the native list/form).
- `static/src/js/backend/archived_reason_ribbon_field.js` + `static/src/xml/backend/archived_reason_ribbon_field.xml` (the shared `ems_archived_reason_ribbon` field widget).
- `tests/test_departure_reason.py`, `tests/test_employee_archived_reason_tour.py` + `static/tests/tours/employee_archived_reason_tour.js`.

## Room-per-schedule-block foundation (no user-visible change yet - lays the groundwork for the import wizard's future "reasignar aulas" step):
- `resource.calendar.attendance.space_id` (the Schedule tab/importer's own weekly block) stopped being a compute always re-derived from the group - it's a plain stored field now, defaulted from the group at creation (`ems_working_schedule_assignation.create()`) but free to diverge afterwards. `ems.attendance_template.sync_from_schedule*`'s `_schedule_lines` now prefers an entry's own `space_id` over the group-derived default, so a one-off room override on a single schedule line survives future syncs instead of being silently overwritten.
- `ems.attendance_session_header._compute_space_id` fixed to read the room from its own `attendance_schedule_id.space_id` (the line) instead of skipping straight to the template's - harmless while both were always forced identical, but would have shown the wrong (stale) room the moment a line's room could genuinely diverge.
- `ems.attendance_template.space_id` relabeled "Session's default space" (was "Space") - it's no longer the authoritative room (that's the schedule line's own field), just the seed value for a new line. Stays editable, stored, unchanged type.
- See `plans/working_schedule_import_redesign.md`'s "Room reassignment" section for the full design and why two earlier drafts of it (a new override model, then a template-grouping-key change) were abandoned in favor of this simpler mechanism.
