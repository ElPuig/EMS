<odoo>
    <data>
        <record model="ir.ui.view" id="view_attendance_notification_tutor_form">
            <field name="name">ems.attendance_notification_tutor.form</field>
            <field name="model">ems.attendance_notification_tutor</field>
            <field name="arch" type="xml">
                <form string="notification">                    
                    <sheet>     
                         <group string="Main data" col="3">                                 
                            <group>
                                <field name="tutor_id"  widget="many2one_avatar" readonly="True" />
                            </group>  
                            <group>
                                <field name="date" readonly="True" />
                            </group>
                            <group >
                                <field name="sent_date" readonly="True" />
                            </group>                   
                        </group>

                        <!-- <notebook>
                            <page string="Notifications">                                
                                <field name="attendance_notification_line_ids">
                                    <list default_order="student_id asc" create="False" edit="False" delete="False" no_open="True">                                                                               
                                        <field name="student_id" widget="many2one_avatar"/>
                                        <field name="status" width="175px" /> -->
                                        <!-- 
                                            TODO: the main idea was to store the values as contacts and use the tags widget to display the email
                                            but was just displaying all the name (student name, contact name). Maybe a custom widget for just the 
                                            email can be developped?
                                        -->
                                        <!-- <field name="send_to"/>
                                        <field name="sent_date"/>
                                    </list>
                                </field>
                            </page>                            
                            <page string="Notes">
                                <field name="notes"/>
                            </page>
                        </notebook> -->
                    </sheet>	
                </form>              
            </field>			
        </record>
    </data>
</odoo>