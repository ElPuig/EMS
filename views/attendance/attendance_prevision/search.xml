<odoo>
    <data>
        <record model="ir.ui.view" id="view_attendance_prevision_search">
            <field name="name">ems.attendance_prevision.search</field>
            <field name="model">ems.attendance_prevision</field>            
            <field name="arch" type="xml">                  
                <search string="Only mine">                                                                             
                    <filter string="Show only mine" name="only_mine" domain="[('teacher_id.user_id.id', '=', uid)]"/>
                </search>           
            </field>		      
        </record>   
    </data>
</odoo>