<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <!-- Embeds the published process map document (company setting). First entry of the
             Quality app: it is the picture everything else in the app hangs off. -->
        <record id="action_quality_process_map" model="ir.actions.client">
            <field name="name">Process map</field>
            <field name="tag">ems_quality_process_map</field>
        </record>

        <menuitem name="Process map" sequence="1" id="menu_quality_process_map"
                  parent="menu_quality" action="action_quality_process_map"/>
    </data>
</odoo>
